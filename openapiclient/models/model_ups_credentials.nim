#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type UpsCredentials* = object
  ## 
  clientId*: string ## OAuth 2.0 client credentials from developer.ups.com.
  clientSecret*: string
  shipperNumber*: Option[string] ## UPS account number; required for label creation, optional for rates/tracking.


# Custom JSON deserialization for UpsCredentials with custom field names
proc to*(node: JsonNode, T: typedesc[UpsCredentials]): UpsCredentials =
  result = UpsCredentials()
  if node.kind == JObject:
    if node.hasKey("client_id"):
      result.clientId = to(node["client_id"], string)
    if node.hasKey("client_secret"):
      result.clientSecret = to(node["client_secret"], string)
    if node.hasKey("shipper_number") and node["shipper_number"].kind != JNull:
      result.shipperNumber = some(to(node["shipper_number"], typeof(result.shipperNumber.get())))

# Custom JSON serialization for UpsCredentials with custom field names
proc `%`*(obj: UpsCredentials): JsonNode =
  result = newJObject()
  result["client_id"] = %obj.clientId
  result["client_secret"] = %obj.clientSecret
  if obj.shipperNumber.isSome():
    result["shipper_number"] = %obj.shipperNumber.get()

