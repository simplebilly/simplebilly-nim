#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type QuartileBand* = object
  ## 
  femaleSharePct*: float64
  hourlyMedian*: string
  maleSharePct*: float64
  quartile*: string


# Custom JSON deserialization for QuartileBand with custom field names
proc to*(node: JsonNode, T: typedesc[QuartileBand]): QuartileBand =
  result = QuartileBand()
  if node.kind == JObject:
    if node.hasKey("female_share_pct"):
      result.femaleSharePct = to(node["female_share_pct"], float64)
    if node.hasKey("hourly_median"):
      result.hourlyMedian = to(node["hourly_median"], string)
    if node.hasKey("male_share_pct"):
      result.maleSharePct = to(node["male_share_pct"], float64)
    if node.hasKey("quartile"):
      result.quartile = to(node["quartile"], string)

# Custom JSON serialization for QuartileBand with custom field names
proc `%`*(obj: QuartileBand): JsonNode =
  result = newJObject()
  result["female_share_pct"] = %obj.femaleSharePct
  result["hourly_median"] = %obj.hourlyMedian
  result["male_share_pct"] = %obj.maleSharePct
  result["quartile"] = %obj.quartile

