#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_shipping_rate

type MethodSuitability* = object
  ## 
  carrier*: string
  rate*: Option[ShippingRate]
  reasons*: seq[string]
  service*: string
  suitable*: bool


# Custom JSON deserialization for MethodSuitability with custom field names
proc to*(node: JsonNode, T: typedesc[MethodSuitability]): MethodSuitability =
  result = MethodSuitability()
  if node.kind == JObject:
    if node.hasKey("carrier"):
      result.carrier = to(node["carrier"], string)
    if node.hasKey("rate") and node["rate"].kind != JNull:
      result.rate = some(to(node["rate"], typeof(result.rate.get())))
    if node.hasKey("reasons"):
      result.reasons = to(node["reasons"], seq[string])
    if node.hasKey("service"):
      result.service = to(node["service"], string)
    if node.hasKey("suitable"):
      result.suitable = to(node["suitable"], bool)

# Custom JSON serialization for MethodSuitability with custom field names
proc `%`*(obj: MethodSuitability): JsonNode =
  result = newJObject()
  result["carrier"] = %obj.carrier
  if obj.rate.isSome():
    result["rate"] = %obj.rate.get()
  result["reasons"] = %obj.reasons
  result["service"] = %obj.service
  result["suitable"] = %obj.suitable

