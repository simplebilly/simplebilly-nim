#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_dhl_credentials
import model_ups_credentials

type ShippingCredentials* = object
  ## Per-tenant credentials for real shipping provider APIs (stored in the `shipping` key of the settings JSON blob). Auth is either OAuth client credentials (UPS) or a user-supplied API key (DHL).
  dhl*: Option[DhlCredentials]
  ups*: Option[UpsCredentials]


# Custom JSON deserialization for ShippingCredentials with custom field names
proc to*(node: JsonNode, T: typedesc[ShippingCredentials]): ShippingCredentials =
  result = ShippingCredentials()
  if node.kind == JObject:
    if node.hasKey("dhl") and node["dhl"].kind != JNull:
      result.dhl = some(to(node["dhl"], typeof(result.dhl.get())))
    if node.hasKey("ups") and node["ups"].kind != JNull:
      result.ups = some(to(node["ups"], typeof(result.ups.get())))

# Custom JSON serialization for ShippingCredentials with custom field names
proc `%`*(obj: ShippingCredentials): JsonNode =
  result = newJObject()
  if obj.dhl.isSome():
    result["dhl"] = %obj.dhl.get()
  if obj.ups.isSome():
    result["ups"] = %obj.ups.get()

