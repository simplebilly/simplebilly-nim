#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_pn_l_item

type IncomeStatement* = object
  ## 
  expenseItems*: seq[PnLItem]
  netIncome*: string
  revenueItems*: seq[PnLItem]
  totalExpenses*: string
  totalRevenue*: string


# Custom JSON deserialization for IncomeStatement with custom field names
proc to*(node: JsonNode, T: typedesc[IncomeStatement]): IncomeStatement =
  result = IncomeStatement()
  if node.kind == JObject:
    if node.hasKey("expense_items"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["expense_items"]
      if arrayNode.kind == JArray:
        result.expenseItems = @[]
        for item in arrayNode.items:
          result.expenseItems.add(to(item, PnLItem))
    if node.hasKey("net_income"):
      result.netIncome = to(node["net_income"], string)
    if node.hasKey("revenue_items"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["revenue_items"]
      if arrayNode.kind == JArray:
        result.revenueItems = @[]
        for item in arrayNode.items:
          result.revenueItems.add(to(item, PnLItem))
    if node.hasKey("total_expenses"):
      result.totalExpenses = to(node["total_expenses"], string)
    if node.hasKey("total_revenue"):
      result.totalRevenue = to(node["total_revenue"], string)

# Custom JSON serialization for IncomeStatement with custom field names
proc `%`*(obj: IncomeStatement): JsonNode =
  result = newJObject()
  result["expense_items"] = %obj.expenseItems
  result["net_income"] = %obj.netIncome
  result["revenue_items"] = %obj.revenueItems
  result["total_expenses"] = %obj.totalExpenses
  result["total_revenue"] = %obj.totalRevenue

