#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_budget_kategorie

type BudgetErgebnis* = object
  ## 
  jahr*: int
  monat*: int
  monatsBudget*: seq[BudgetKategorie]
  prognoseRestjahr*: seq[BudgetKategorie]


# Custom JSON deserialization for BudgetErgebnis with custom field names
proc to*(node: JsonNode, T: typedesc[BudgetErgebnis]): BudgetErgebnis =
  result = BudgetErgebnis()
  if node.kind == JObject:
    if node.hasKey("jahr"):
      result.jahr = to(node["jahr"], int)
    if node.hasKey("monat"):
      result.monat = to(node["monat"], int)
    if node.hasKey("monats_budget"):
      result.monatsBudget = to(node["monats_budget"], seq[BudgetKategorie])
    if node.hasKey("prognose_restjahr"):
      result.prognoseRestjahr = to(node["prognose_restjahr"], seq[BudgetKategorie])

# Custom JSON serialization for BudgetErgebnis with custom field names
proc `%`*(obj: BudgetErgebnis): JsonNode =
  result = newJObject()
  result["jahr"] = %obj.jahr
  result["monat"] = %obj.monat
  result["monats_budget"] = %obj.monatsBudget
  result["prognose_restjahr"] = %obj.prognoseRestjahr

