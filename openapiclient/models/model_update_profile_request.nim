#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type UpdateProfileRequest* = object
  ## 
  avatarUrl*: Option[string]
  firstName*: Option[string]
  lastName*: Option[string]
  name*: Option[string]


# Custom JSON deserialization for UpdateProfileRequest with custom field names
proc to*(node: JsonNode, T: typedesc[UpdateProfileRequest]): UpdateProfileRequest =
  result = UpdateProfileRequest()
  if node.kind == JObject:
    if node.hasKey("avatar_url") and node["avatar_url"].kind != JNull:
      result.avatarUrl = some(to(node["avatar_url"], typeof(result.avatarUrl.get())))
    if node.hasKey("first_name") and node["first_name"].kind != JNull:
      result.firstName = some(to(node["first_name"], typeof(result.firstName.get())))
    if node.hasKey("last_name") and node["last_name"].kind != JNull:
      result.lastName = some(to(node["last_name"], typeof(result.lastName.get())))
    if node.hasKey("name") and node["name"].kind != JNull:
      result.name = some(to(node["name"], typeof(result.name.get())))

# Custom JSON serialization for UpdateProfileRequest with custom field names
proc `%`*(obj: UpdateProfileRequest): JsonNode =
  result = newJObject()
  if obj.avatarUrl.isSome():
    result["avatar_url"] = %obj.avatarUrl.get()
  if obj.firstName.isSome():
    result["first_name"] = %obj.firstName.get()
  if obj.lastName.isSome():
    result["last_name"] = %obj.lastName.get()
  if obj.name.isSome():
    result["name"] = %obj.name.get()

