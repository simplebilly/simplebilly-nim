#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type BomStatus* {.pure.} = enum
  Draft
  Active
  Archived

func `%`*(v: BomStatus): JsonNode =
  result = case v:
    of BomStatus.Draft: %"draft"
    of BomStatus.Active: %"active"
    of BomStatus.Archived: %"archived"

func `$`*(v: BomStatus): string =
  result = case v:
    of BomStatus.Draft: $("draft")
    of BomStatus.Active: $("active")
    of BomStatus.Archived: $("archived")

proc to*(node: JsonNode, T: typedesc[BomStatus]): BomStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum BomStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("draft"):
    return BomStatus.Draft
  of $("active"):
    return BomStatus.Active
  of $("archived"):
    return BomStatus.Archived
  else:
    raise newException(ValueError, "Invalid enum value for BomStatus: " & strVal)

