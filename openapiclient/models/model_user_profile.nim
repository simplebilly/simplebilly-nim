#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type UserProfile* = object
  ## 
  createdAt*: string
  email*: string
  emailVerified*: bool
  firstName*: string
  fullName*: string
  id*: string
  lastName*: string


# Custom JSON deserialization for UserProfile with custom field names
proc to*(node: JsonNode, T: typedesc[UserProfile]): UserProfile =
  result = UserProfile()
  if node.kind == JObject:
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], string)
    if node.hasKey("email"):
      result.email = to(node["email"], string)
    if node.hasKey("email_verified"):
      result.emailVerified = to(node["email_verified"], bool)
    if node.hasKey("first_name"):
      result.firstName = to(node["first_name"], string)
    if node.hasKey("full_name"):
      result.fullName = to(node["full_name"], string)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("last_name"):
      result.lastName = to(node["last_name"], string)

# Custom JSON serialization for UserProfile with custom field names
proc `%`*(obj: UserProfile): JsonNode =
  result = newJObject()
  result["created_at"] = %obj.createdAt
  result["email"] = %obj.email
  result["email_verified"] = %obj.emailVerified
  result["first_name"] = %obj.firstName
  result["full_name"] = %obj.fullName
  result["id"] = %obj.id
  result["last_name"] = %obj.lastName

