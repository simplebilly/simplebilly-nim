#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type StockTransferStatus* {.pure.} = enum
  Draft
  Completed
  Cancelled

func `%`*(v: StockTransferStatus): JsonNode =
  result = case v:
    of StockTransferStatus.Draft: %"draft"
    of StockTransferStatus.Completed: %"completed"
    of StockTransferStatus.Cancelled: %"cancelled"

func `$`*(v: StockTransferStatus): string =
  result = case v:
    of StockTransferStatus.Draft: $("draft")
    of StockTransferStatus.Completed: $("completed")
    of StockTransferStatus.Cancelled: $("cancelled")

proc to*(node: JsonNode, T: typedesc[StockTransferStatus]): StockTransferStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum StockTransferStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("draft"):
    return StockTransferStatus.Draft
  of $("completed"):
    return StockTransferStatus.Completed
  of $("cancelled"):
    return StockTransferStatus.Cancelled
  else:
    raise newException(ValueError, "Invalid enum value for StockTransferStatus: " & strVal)

