#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type

type InvoiceLineItem* = object
  ## 
  articleNumber*: Option[string]
  description*: string
  discountAmount*: Option[string]
  discountPercentage*: Option[string]
  inputVatDeductible*: Option[bool]
  inputVatRate*: Option[string]
  isIntraCommunityAcquisition*: Option[bool]
  isMargin25a*: Option[bool]
  ledgerAccount*: Option[string]
  lineTotal*: string
  lineTotalGross*: Option[string]
  margin25aPurchasePrice*: Option[string]
  meterPointId*: Option[string]
  position*: int64
  priceComponents*: Option[JsonNode]
  productId*: Option[string]
  productSku*: Option[string]
  quantity*: string
  supplierArticleNumber*: Option[string]
  taxRate*: Option[string]
  unit*: Option[JsonNode]
  unitPrice*: string
  usageDataId*: Option[string]
  vatRateNominal*: Option[string]
  vatSpecialCase*: Option[string]


# Custom JSON deserialization for InvoiceLineItem with custom field names
proc to*(node: JsonNode, T: typedesc[InvoiceLineItem]): InvoiceLineItem =
  result = InvoiceLineItem()
  if node.kind == JObject:
    if node.hasKey("article_number") and node["article_number"].kind != JNull:
      result.articleNumber = some(to(node["article_number"], typeof(result.articleNumber.get())))
    if node.hasKey("description"):
      result.description = to(node["description"], string)
    if node.hasKey("discount_amount") and node["discount_amount"].kind != JNull:
      result.discountAmount = some(to(node["discount_amount"], typeof(result.discountAmount.get())))
    if node.hasKey("discount_percentage") and node["discount_percentage"].kind != JNull:
      result.discountPercentage = some(to(node["discount_percentage"], typeof(result.discountPercentage.get())))
    if node.hasKey("input_vat_deductible") and node["input_vat_deductible"].kind != JNull:
      result.inputVatDeductible = some(to(node["input_vat_deductible"], typeof(result.inputVatDeductible.get())))
    if node.hasKey("input_vat_rate") and node["input_vat_rate"].kind != JNull:
      result.inputVatRate = some(to(node["input_vat_rate"], typeof(result.inputVatRate.get())))
    if node.hasKey("is_intra_community_acquisition") and node["is_intra_community_acquisition"].kind != JNull:
      result.isIntraCommunityAcquisition = some(to(node["is_intra_community_acquisition"], typeof(result.isIntraCommunityAcquisition.get())))
    if node.hasKey("is_margin_25a") and node["is_margin_25a"].kind != JNull:
      result.isMargin25a = some(to(node["is_margin_25a"], typeof(result.isMargin25a.get())))
    if node.hasKey("ledger_account") and node["ledger_account"].kind != JNull:
      result.ledgerAccount = some(to(node["ledger_account"], typeof(result.ledgerAccount.get())))
    if node.hasKey("line_total"):
      result.lineTotal = to(node["line_total"], string)
    if node.hasKey("line_total_gross") and node["line_total_gross"].kind != JNull:
      result.lineTotalGross = some(to(node["line_total_gross"], typeof(result.lineTotalGross.get())))
    if node.hasKey("margin_25a_purchase_price") and node["margin_25a_purchase_price"].kind != JNull:
      result.margin25aPurchasePrice = some(to(node["margin_25a_purchase_price"], typeof(result.margin25aPurchasePrice.get())))
    if node.hasKey("meter_point_id") and node["meter_point_id"].kind != JNull:
      result.meterPointId = some(to(node["meter_point_id"], typeof(result.meterPointId.get())))
    if node.hasKey("position"):
      result.position = to(node["position"], int64)
    if node.hasKey("price_components") and node["price_components"].kind != JNull:
      result.priceComponents = some(to(node["price_components"], typeof(result.priceComponents.get())))
    if node.hasKey("product_id") and node["product_id"].kind != JNull:
      result.productId = some(to(node["product_id"], typeof(result.productId.get())))
    if node.hasKey("product_sku") and node["product_sku"].kind != JNull:
      result.productSku = some(to(node["product_sku"], typeof(result.productSku.get())))
    if node.hasKey("quantity"):
      result.quantity = to(node["quantity"], string)
    if node.hasKey("supplier_article_number") and node["supplier_article_number"].kind != JNull:
      result.supplierArticleNumber = some(to(node["supplier_article_number"], typeof(result.supplierArticleNumber.get())))
    if node.hasKey("tax_rate") and node["tax_rate"].kind != JNull:
      result.taxRate = some(to(node["tax_rate"], typeof(result.taxRate.get())))
    if node.hasKey("unit") and node["unit"].kind != JNull:
      result.unit = some(to(node["unit"], typeof(result.unit.get())))
    if node.hasKey("unit_price"):
      result.unitPrice = to(node["unit_price"], string)
    if node.hasKey("usage_data_id") and node["usage_data_id"].kind != JNull:
      result.usageDataId = some(to(node["usage_data_id"], typeof(result.usageDataId.get())))
    if node.hasKey("vat_rate_nominal") and node["vat_rate_nominal"].kind != JNull:
      result.vatRateNominal = some(to(node["vat_rate_nominal"], typeof(result.vatRateNominal.get())))
    if node.hasKey("vat_special_case") and node["vat_special_case"].kind != JNull:
      result.vatSpecialCase = some(to(node["vat_special_case"], typeof(result.vatSpecialCase.get())))

# Custom JSON serialization for InvoiceLineItem with custom field names
proc `%`*(obj: InvoiceLineItem): JsonNode =
  result = newJObject()
  if obj.articleNumber.isSome():
    result["article_number"] = %obj.articleNumber.get()
  result["description"] = %obj.description
  if obj.discountAmount.isSome():
    result["discount_amount"] = %obj.discountAmount.get()
  if obj.discountPercentage.isSome():
    result["discount_percentage"] = %obj.discountPercentage.get()
  if obj.inputVatDeductible.isSome():
    result["input_vat_deductible"] = %obj.inputVatDeductible.get()
  if obj.inputVatRate.isSome():
    result["input_vat_rate"] = %obj.inputVatRate.get()
  if obj.isIntraCommunityAcquisition.isSome():
    result["is_intra_community_acquisition"] = %obj.isIntraCommunityAcquisition.get()
  if obj.isMargin25a.isSome():
    result["is_margin_25a"] = %obj.isMargin25a.get()
  if obj.ledgerAccount.isSome():
    result["ledger_account"] = %obj.ledgerAccount.get()
  result["line_total"] = %obj.lineTotal
  if obj.lineTotalGross.isSome():
    result["line_total_gross"] = %obj.lineTotalGross.get()
  if obj.margin25aPurchasePrice.isSome():
    result["margin_25a_purchase_price"] = %obj.margin25aPurchasePrice.get()
  if obj.meterPointId.isSome():
    result["meter_point_id"] = %obj.meterPointId.get()
  result["position"] = %obj.position
  if obj.priceComponents.isSome():
    result["price_components"] = %obj.priceComponents.get()
  if obj.productId.isSome():
    result["product_id"] = %obj.productId.get()
  if obj.productSku.isSome():
    result["product_sku"] = %obj.productSku.get()
  result["quantity"] = %obj.quantity
  if obj.supplierArticleNumber.isSome():
    result["supplier_article_number"] = %obj.supplierArticleNumber.get()
  if obj.taxRate.isSome():
    result["tax_rate"] = %obj.taxRate.get()
  if obj.unit.isSome():
    result["unit"] = %obj.unit.get()
  result["unit_price"] = %obj.unitPrice
  if obj.usageDataId.isSome():
    result["usage_data_id"] = %obj.usageDataId.get()
  if obj.vatRateNominal.isSome():
    result["vat_rate_nominal"] = %obj.vatRateNominal.get()
  if obj.vatSpecialCase.isSome():
    result["vat_special_case"] = %obj.vatSpecialCase.get()

