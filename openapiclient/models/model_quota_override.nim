#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_quota_override_features

type QuotaOverride* = object
  ## Schema of the `tenants.quotas` JSON override column. Any field that is present overrides the plan-derived value.
  features*: Option[QuotaOverrideFeatures]
  maxConnectors*: Option[int]
  maxInvoicesPerMonth*: Option[int64]
  maxUsers*: Option[int]
  metered*: Option[Table[string, int64]]
  plan*: Option[string] ## Custom plan id; unknown ids resolve to enterprise limits.


# Custom JSON deserialization for QuotaOverride with custom field names
proc to*(node: JsonNode, T: typedesc[QuotaOverride]): QuotaOverride =
  result = QuotaOverride()
  if node.kind == JObject:
    if node.hasKey("features") and node["features"].kind != JNull:
      result.features = some(to(node["features"], typeof(result.features.get())))
    if node.hasKey("max_connectors") and node["max_connectors"].kind != JNull:
      result.maxConnectors = some(to(node["max_connectors"], typeof(result.maxConnectors.get())))
    if node.hasKey("max_invoices_per_month") and node["max_invoices_per_month"].kind != JNull:
      result.maxInvoicesPerMonth = some(to(node["max_invoices_per_month"], typeof(result.maxInvoicesPerMonth.get())))
    if node.hasKey("max_users") and node["max_users"].kind != JNull:
      result.maxUsers = some(to(node["max_users"], typeof(result.maxUsers.get())))
    if node.hasKey("metered") and node["metered"].kind != JNull:
      result.metered = some(to(node["metered"], typeof(result.metered.get())))
    if node.hasKey("plan") and node["plan"].kind != JNull:
      result.plan = some(to(node["plan"], typeof(result.plan.get())))

# Custom JSON serialization for QuotaOverride with custom field names
proc `%`*(obj: QuotaOverride): JsonNode =
  result = newJObject()
  if obj.features.isSome():
    result["features"] = %obj.features.get()
  if obj.maxConnectors.isSome():
    result["max_connectors"] = %obj.maxConnectors.get()
  if obj.maxInvoicesPerMonth.isSome():
    result["max_invoices_per_month"] = %obj.maxInvoicesPerMonth.get()
  if obj.maxUsers.isSome():
    result["max_users"] = %obj.maxUsers.get()
  if obj.metered.isSome():
    result["metered"] = %obj.metered.get()
  if obj.plan.isSome():
    result["plan"] = %obj.plan.get()

