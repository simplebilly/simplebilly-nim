#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type TargetProgress* = object
  ## 
  baseValue*: float64
  baseYear*: int
  description*: string
  id*: string
  progressPct*: Option[float64] ## Current year's emissions for the scope as % of the target. None when no data.
  scope*: string
  targetValue*: float64
  targetYear*: int


# Custom JSON deserialization for TargetProgress with custom field names
proc to*(node: JsonNode, T: typedesc[TargetProgress]): TargetProgress =
  result = TargetProgress()
  if node.kind == JObject:
    if node.hasKey("base_value"):
      result.baseValue = to(node["base_value"], float64)
    if node.hasKey("base_year"):
      result.baseYear = to(node["base_year"], int)
    if node.hasKey("description"):
      result.description = to(node["description"], string)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("progress_pct") and node["progress_pct"].kind != JNull:
      result.progressPct = some(to(node["progress_pct"], typeof(result.progressPct.get())))
    if node.hasKey("scope"):
      result.scope = to(node["scope"], string)
    if node.hasKey("target_value"):
      result.targetValue = to(node["target_value"], float64)
    if node.hasKey("target_year"):
      result.targetYear = to(node["target_year"], int)

# Custom JSON serialization for TargetProgress with custom field names
proc `%`*(obj: TargetProgress): JsonNode =
  result = newJObject()
  result["base_value"] = %obj.baseValue
  result["base_year"] = %obj.baseYear
  result["description"] = %obj.description
  result["id"] = %obj.id
  if obj.progressPct.isSome():
    result["progress_pct"] = %obj.progressPct.get()
  result["scope"] = %obj.scope
  result["target_value"] = %obj.targetValue
  result["target_year"] = %obj.targetYear

