#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type SupplierInvoiceStatus* {.pure.} = enum
  Draft
  Matched
  HasVariances
  Posted
  Cancelled

func `%`*(v: SupplierInvoiceStatus): JsonNode =
  result = case v:
    of SupplierInvoiceStatus.Draft: %"draft"
    of SupplierInvoiceStatus.Matched: %"matched"
    of SupplierInvoiceStatus.HasVariances: %"has_variances"
    of SupplierInvoiceStatus.Posted: %"posted"
    of SupplierInvoiceStatus.Cancelled: %"cancelled"

func `$`*(v: SupplierInvoiceStatus): string =
  result = case v:
    of SupplierInvoiceStatus.Draft: $("draft")
    of SupplierInvoiceStatus.Matched: $("matched")
    of SupplierInvoiceStatus.HasVariances: $("has_variances")
    of SupplierInvoiceStatus.Posted: $("posted")
    of SupplierInvoiceStatus.Cancelled: $("cancelled")

proc to*(node: JsonNode, T: typedesc[SupplierInvoiceStatus]): SupplierInvoiceStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum SupplierInvoiceStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("draft"):
    return SupplierInvoiceStatus.Draft
  of $("matched"):
    return SupplierInvoiceStatus.Matched
  of $("has_variances"):
    return SupplierInvoiceStatus.HasVariances
  of $("posted"):
    return SupplierInvoiceStatus.Posted
  of $("cancelled"):
    return SupplierInvoiceStatus.Cancelled
  else:
    raise newException(ValueError, "Invalid enum value for SupplierInvoiceStatus: " & strVal)

