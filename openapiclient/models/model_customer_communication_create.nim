#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type
import model_communication_channel
import model_communication_direction

type CustomerCommunicationCreate* = object
  ## 
  body*: Option[string] ## The message body, call summary or note text.
  channel*: CommunicationChannel
  contactId*: string ## The contact (customer/supplier) this communication belongs to. References the contact entity.
  counterparty*: Option[string] ## Email/phone of the counterparty, if applicable.
  direction*: CommunicationDirection
  occurredAt*: Option[string] ## When the communication happened (defaults to now on create).
  subject*: Option[string]
  tags*: Option[JsonNode] ## Free-form tags, e.g. `[\"follow-up-required\"]`.

