#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_shipping_rate

type RateResponse* = object
  ## 
  rates*: seq[ShippingRate]


# Custom JSON deserialization for RateResponse with custom field names
proc to*(node: JsonNode, T: typedesc[RateResponse]): RateResponse =
  result = RateResponse()
  if node.kind == JObject:
    if node.hasKey("rates"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["rates"]
      if arrayNode.kind == JArray:
        result.rates = @[]
        for item in arrayNode.items:
          result.rates.add(to(item, ShippingRate))

# Custom JSON serialization for RateResponse with custom field names
proc `%`*(obj: RateResponse): JsonNode =
  result = newJObject()
  result["rates"] = %obj.rates

