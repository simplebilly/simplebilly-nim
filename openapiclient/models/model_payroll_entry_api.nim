#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_employee
import model_payroll_run_status

type PayrollEntryApi* = object
  ## 
  avEmployee*: string
  avEmployer*: string
  churchTaxAmount*: string
  employee*: Option[Employee]
  employeeId*: string
  entryId*: string
  extraPaymentReason*: Option[string]
  extraPayments*: string
  grossSalary*: string
  kvEmployee*: string
  kvEmployer*: string
  lohnsteuer*: string
  netSalary*: string
  notes*: Option[string]
  pvEmployee*: string
  pvEmployer*: string
  runId*: string
  rvEmployee*: string
  rvEmployer*: string
  sickDays*: int
  soli*: string
  status*: PayrollRunStatus
  totalDeductions*: string
  totalEmployerCost*: string
  vacationDaysUsed*: int


# Custom JSON deserialization for PayrollEntryApi with custom field names
proc to*(node: JsonNode, T: typedesc[PayrollEntryApi]): PayrollEntryApi =
  result = PayrollEntryApi()
  if node.kind == JObject:
    if node.hasKey("av_employee"):
      result.avEmployee = to(node["av_employee"], string)
    if node.hasKey("av_employer"):
      result.avEmployer = to(node["av_employer"], string)
    if node.hasKey("church_tax_amount"):
      result.churchTaxAmount = to(node["church_tax_amount"], string)
    if node.hasKey("employee") and node["employee"].kind != JNull:
      result.employee = some(to(node["employee"], typeof(result.employee.get())))
    if node.hasKey("employee_id"):
      result.employeeId = to(node["employee_id"], string)
    if node.hasKey("entry_id"):
      result.entryId = to(node["entry_id"], string)
    if node.hasKey("extra_payment_reason") and node["extra_payment_reason"].kind != JNull:
      result.extraPaymentReason = some(to(node["extra_payment_reason"], typeof(result.extraPaymentReason.get())))
    if node.hasKey("extra_payments"):
      result.extraPayments = to(node["extra_payments"], string)
    if node.hasKey("gross_salary"):
      result.grossSalary = to(node["gross_salary"], string)
    if node.hasKey("kv_employee"):
      result.kvEmployee = to(node["kv_employee"], string)
    if node.hasKey("kv_employer"):
      result.kvEmployer = to(node["kv_employer"], string)
    if node.hasKey("lohnsteuer"):
      result.lohnsteuer = to(node["lohnsteuer"], string)
    if node.hasKey("net_salary"):
      result.netSalary = to(node["net_salary"], string)
    if node.hasKey("notes") and node["notes"].kind != JNull:
      result.notes = some(to(node["notes"], typeof(result.notes.get())))
    if node.hasKey("pv_employee"):
      result.pvEmployee = to(node["pv_employee"], string)
    if node.hasKey("pv_employer"):
      result.pvEmployer = to(node["pv_employer"], string)
    if node.hasKey("run_id"):
      result.runId = to(node["run_id"], string)
    if node.hasKey("rv_employee"):
      result.rvEmployee = to(node["rv_employee"], string)
    if node.hasKey("rv_employer"):
      result.rvEmployer = to(node["rv_employer"], string)
    if node.hasKey("sick_days"):
      result.sickDays = to(node["sick_days"], int)
    if node.hasKey("soli"):
      result.soli = to(node["soli"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], PayrollRunStatus)
    if node.hasKey("total_deductions"):
      result.totalDeductions = to(node["total_deductions"], string)
    if node.hasKey("total_employer_cost"):
      result.totalEmployerCost = to(node["total_employer_cost"], string)
    if node.hasKey("vacation_days_used"):
      result.vacationDaysUsed = to(node["vacation_days_used"], int)

# Custom JSON serialization for PayrollEntryApi with custom field names
proc `%`*(obj: PayrollEntryApi): JsonNode =
  result = newJObject()
  result["av_employee"] = %obj.avEmployee
  result["av_employer"] = %obj.avEmployer
  result["church_tax_amount"] = %obj.churchTaxAmount
  if obj.employee.isSome():
    result["employee"] = %obj.employee.get()
  result["employee_id"] = %obj.employeeId
  result["entry_id"] = %obj.entryId
  if obj.extraPaymentReason.isSome():
    result["extra_payment_reason"] = %obj.extraPaymentReason.get()
  result["extra_payments"] = %obj.extraPayments
  result["gross_salary"] = %obj.grossSalary
  result["kv_employee"] = %obj.kvEmployee
  result["kv_employer"] = %obj.kvEmployer
  result["lohnsteuer"] = %obj.lohnsteuer
  result["net_salary"] = %obj.netSalary
  if obj.notes.isSome():
    result["notes"] = %obj.notes.get()
  result["pv_employee"] = %obj.pvEmployee
  result["pv_employer"] = %obj.pvEmployer
  result["run_id"] = %obj.runId
  result["rv_employee"] = %obj.rvEmployee
  result["rv_employer"] = %obj.rvEmployer
  result["sick_days"] = %obj.sickDays
  result["soli"] = %obj.soli
  result["status"] = %obj.status
  result["total_deductions"] = %obj.totalDeductions
  result["total_employer_cost"] = %obj.totalEmployerCost
  result["vacation_days_used"] = %obj.vacationDaysUsed

