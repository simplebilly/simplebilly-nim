#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type PosTableStatus* {.pure.} = enum
  Free
  Occupied

func `%`*(v: PosTableStatus): JsonNode =
  result = case v:
    of PosTableStatus.Free: %"free"
    of PosTableStatus.Occupied: %"occupied"

func `$`*(v: PosTableStatus): string =
  result = case v:
    of PosTableStatus.Free: $("free")
    of PosTableStatus.Occupied: $("occupied")

proc to*(node: JsonNode, T: typedesc[PosTableStatus]): PosTableStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum PosTableStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("free"):
    return PosTableStatus.Free
  of $("occupied"):
    return PosTableStatus.Occupied
  else:
    raise newException(ValueError, "Invalid enum value for PosTableStatus: " & strVal)

