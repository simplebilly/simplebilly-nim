#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type CashflowReport* = object
  ## 
  closingBalance*: float64
  financingCashflow*: float64
  investingCashflow*: float64
  netCashflow*: float64
  openingBalance*: float64
  operatingCashflow*: float64
  period*: string


# Custom JSON deserialization for CashflowReport with custom field names
proc to*(node: JsonNode, T: typedesc[CashflowReport]): CashflowReport =
  result = CashflowReport()
  if node.kind == JObject:
    if node.hasKey("closing_balance"):
      result.closingBalance = to(node["closing_balance"], float64)
    if node.hasKey("financing_cashflow"):
      result.financingCashflow = to(node["financing_cashflow"], float64)
    if node.hasKey("investing_cashflow"):
      result.investingCashflow = to(node["investing_cashflow"], float64)
    if node.hasKey("net_cashflow"):
      result.netCashflow = to(node["net_cashflow"], float64)
    if node.hasKey("opening_balance"):
      result.openingBalance = to(node["opening_balance"], float64)
    if node.hasKey("operating_cashflow"):
      result.operatingCashflow = to(node["operating_cashflow"], float64)
    if node.hasKey("period"):
      result.period = to(node["period"], string)

# Custom JSON serialization for CashflowReport with custom field names
proc `%`*(obj: CashflowReport): JsonNode =
  result = newJObject()
  result["closing_balance"] = %obj.closingBalance
  result["financing_cashflow"] = %obj.financingCashflow
  result["investing_cashflow"] = %obj.investingCashflow
  result["net_cashflow"] = %obj.netCashflow
  result["opening_balance"] = %obj.openingBalance
  result["operating_cashflow"] = %obj.operatingCashflow
  result["period"] = %obj.period

