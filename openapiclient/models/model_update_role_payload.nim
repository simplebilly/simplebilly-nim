#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type UpdateRolePayload* = object
  ## 
  role*: string
  syncPermissions*: Option[bool]


# Custom JSON deserialization for UpdateRolePayload with custom field names
proc to*(node: JsonNode, T: typedesc[UpdateRolePayload]): UpdateRolePayload =
  result = UpdateRolePayload()
  if node.kind == JObject:
    if node.hasKey("role"):
      result.role = to(node["role"], string)
    if node.hasKey("sync_permissions") and node["sync_permissions"].kind != JNull:
      result.syncPermissions = some(to(node["sync_permissions"], typeof(result.syncPermissions.get())))

# Custom JSON serialization for UpdateRolePayload with custom field names
proc `%`*(obj: UpdateRolePayload): JsonNode =
  result = newJObject()
  result["role"] = %obj.role
  if obj.syncPermissions.isSome():
    result["sync_permissions"] = %obj.syncPermissions.get()

