#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ProductStock* = object
  ## 
  name*: string
  productId*: string
  sku*: string
  stockQuantity*: Option[int64]


# Custom JSON deserialization for ProductStock with custom field names
proc to*(node: JsonNode, T: typedesc[ProductStock]): ProductStock =
  result = ProductStock()
  if node.kind == JObject:
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("product_id"):
      result.productId = to(node["product_id"], string)
    if node.hasKey("sku"):
      result.sku = to(node["sku"], string)
    if node.hasKey("stock_quantity") and node["stock_quantity"].kind != JNull:
      result.stockQuantity = some(to(node["stock_quantity"], typeof(result.stockQuantity.get())))

# Custom JSON serialization for ProductStock with custom field names
proc `%`*(obj: ProductStock): JsonNode =
  result = newJObject()
  result["name"] = %obj.name
  result["product_id"] = %obj.productId
  result["sku"] = %obj.sku
  if obj.stockQuantity.isSome():
    result["stock_quantity"] = %obj.stockQuantity.get()

