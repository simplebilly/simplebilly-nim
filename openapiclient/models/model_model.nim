#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Model* = object
  ## 
  backupCodes*: seq[string]
  createdAt*: string
  deletedAt*: Option[string]
  email*: string
  emailVerified*: bool
  id*: string
  isActive*: bool
  isTotpEnabled*: bool
  lastLogin*: Option[string]
  name*: string
  oauthId*: Option[string]
  oauthProvider*: Option[string]
  passwordChangedAt*: Option[string] ## Set on password change; auth/refresh tokens issued before this timestamp are rejected by the auth middleware.
  passwordHash*: string
  picture*: Option[string]
  privacyAcceptedAt*: Option[string] ## When the user accepted the data privacy policy (GDPR consent record).
  totpSecret*: Option[string]
  updatedAt*: string


# Custom JSON deserialization for Model with custom field names
proc to*(node: JsonNode, T: typedesc[Model]): Model =
  result = Model()
  if node.kind == JObject:
    if node.hasKey("backup_codes"):
      result.backupCodes = to(node["backup_codes"], seq[string])
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], string)
    if node.hasKey("deleted_at") and node["deleted_at"].kind != JNull:
      result.deletedAt = some(to(node["deleted_at"], typeof(result.deletedAt.get())))
    if node.hasKey("email"):
      result.email = to(node["email"], string)
    if node.hasKey("email_verified"):
      result.emailVerified = to(node["email_verified"], bool)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("is_active"):
      result.isActive = to(node["is_active"], bool)
    if node.hasKey("is_totp_enabled"):
      result.isTotpEnabled = to(node["is_totp_enabled"], bool)
    if node.hasKey("last_login") and node["last_login"].kind != JNull:
      result.lastLogin = some(to(node["last_login"], typeof(result.lastLogin.get())))
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("oauth_id") and node["oauth_id"].kind != JNull:
      result.oauthId = some(to(node["oauth_id"], typeof(result.oauthId.get())))
    if node.hasKey("oauth_provider") and node["oauth_provider"].kind != JNull:
      result.oauthProvider = some(to(node["oauth_provider"], typeof(result.oauthProvider.get())))
    if node.hasKey("password_changed_at") and node["password_changed_at"].kind != JNull:
      result.passwordChangedAt = some(to(node["password_changed_at"], typeof(result.passwordChangedAt.get())))
    if node.hasKey("password_hash"):
      result.passwordHash = to(node["password_hash"], string)
    if node.hasKey("picture") and node["picture"].kind != JNull:
      result.picture = some(to(node["picture"], typeof(result.picture.get())))
    if node.hasKey("privacy_accepted_at") and node["privacy_accepted_at"].kind != JNull:
      result.privacyAcceptedAt = some(to(node["privacy_accepted_at"], typeof(result.privacyAcceptedAt.get())))
    if node.hasKey("totp_secret") and node["totp_secret"].kind != JNull:
      result.totpSecret = some(to(node["totp_secret"], typeof(result.totpSecret.get())))
    if node.hasKey("updated_at"):
      result.updatedAt = to(node["updated_at"], string)

# Custom JSON serialization for Model with custom field names
proc `%`*(obj: Model): JsonNode =
  result = newJObject()
  result["backup_codes"] = %obj.backupCodes
  result["created_at"] = %obj.createdAt
  if obj.deletedAt.isSome():
    result["deleted_at"] = %obj.deletedAt.get()
  result["email"] = %obj.email
  result["email_verified"] = %obj.emailVerified
  result["id"] = %obj.id
  result["is_active"] = %obj.isActive
  result["is_totp_enabled"] = %obj.isTotpEnabled
  if obj.lastLogin.isSome():
    result["last_login"] = %obj.lastLogin.get()
  result["name"] = %obj.name
  if obj.oauthId.isSome():
    result["oauth_id"] = %obj.oauthId.get()
  if obj.oauthProvider.isSome():
    result["oauth_provider"] = %obj.oauthProvider.get()
  if obj.passwordChangedAt.isSome():
    result["password_changed_at"] = %obj.passwordChangedAt.get()
  result["password_hash"] = %obj.passwordHash
  if obj.picture.isSome():
    result["picture"] = %obj.picture.get()
  if obj.privacyAcceptedAt.isSome():
    result["privacy_accepted_at"] = %obj.privacyAcceptedAt.get()
  if obj.totpSecret.isSome():
    result["totp_secret"] = %obj.totpSecret.get()
  result["updated_at"] = %obj.updatedAt

