#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type EmissionFactorResponse* = object
  ## 
  categoryId*: string
  kgCo2ePerUnit*: float64
  nameDe*: string
  source*: string
  unit*: string
  version*: string


# Custom JSON deserialization for EmissionFactorResponse with custom field names
proc to*(node: JsonNode, T: typedesc[EmissionFactorResponse]): EmissionFactorResponse =
  result = EmissionFactorResponse()
  if node.kind == JObject:
    if node.hasKey("category_id"):
      result.categoryId = to(node["category_id"], string)
    if node.hasKey("kg_co2e_per_unit"):
      result.kgCo2ePerUnit = to(node["kg_co2e_per_unit"], float64)
    if node.hasKey("name_de"):
      result.nameDe = to(node["name_de"], string)
    if node.hasKey("source"):
      result.source = to(node["source"], string)
    if node.hasKey("unit"):
      result.unit = to(node["unit"], string)
    if node.hasKey("version"):
      result.version = to(node["version"], string)

# Custom JSON serialization for EmissionFactorResponse with custom field names
proc `%`*(obj: EmissionFactorResponse): JsonNode =
  result = newJObject()
  result["category_id"] = %obj.categoryId
  result["kg_co2e_per_unit"] = %obj.kgCo2ePerUnit
  result["name_de"] = %obj.nameDe
  result["source"] = %obj.source
  result["unit"] = %obj.unit
  result["version"] = %obj.version

