#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_reminder_level

type OpenItem* = object
  ## 
  amountDue*: string
  amountPaid*: string
  customerId*: Option[string]
  daysOverdue*: Option[int64]
  dueDate*: Option[string]
  invoiceId*: string
  invoiceNumber*: string
  issueDate*: string
  openAmount*: string
  reminderLevel*: ReminderLevel


# Custom JSON deserialization for OpenItem with custom field names
proc to*(node: JsonNode, T: typedesc[OpenItem]): OpenItem =
  result = OpenItem()
  if node.kind == JObject:
    if node.hasKey("amount_due"):
      result.amountDue = to(node["amount_due"], string)
    if node.hasKey("amount_paid"):
      result.amountPaid = to(node["amount_paid"], string)
    if node.hasKey("customer_id") and node["customer_id"].kind != JNull:
      result.customerId = some(to(node["customer_id"], typeof(result.customerId.get())))
    if node.hasKey("days_overdue") and node["days_overdue"].kind != JNull:
      result.daysOverdue = some(to(node["days_overdue"], typeof(result.daysOverdue.get())))
    if node.hasKey("due_date") and node["due_date"].kind != JNull:
      result.dueDate = some(to(node["due_date"], typeof(result.dueDate.get())))
    if node.hasKey("invoice_id"):
      result.invoiceId = to(node["invoice_id"], string)
    if node.hasKey("invoice_number"):
      result.invoiceNumber = to(node["invoice_number"], string)
    if node.hasKey("issue_date"):
      result.issueDate = to(node["issue_date"], string)
    if node.hasKey("open_amount"):
      result.openAmount = to(node["open_amount"], string)
    if node.hasKey("reminder_level"):
      result.reminderLevel = to(node["reminder_level"], ReminderLevel)

# Custom JSON serialization for OpenItem with custom field names
proc `%`*(obj: OpenItem): JsonNode =
  result = newJObject()
  result["amount_due"] = %obj.amountDue
  result["amount_paid"] = %obj.amountPaid
  if obj.customerId.isSome():
    result["customer_id"] = %obj.customerId.get()
  if obj.daysOverdue.isSome():
    result["days_overdue"] = %obj.daysOverdue.get()
  if obj.dueDate.isSome():
    result["due_date"] = %obj.dueDate.get()
  result["invoice_id"] = %obj.invoiceId
  result["invoice_number"] = %obj.invoiceNumber
  result["issue_date"] = %obj.issueDate
  result["open_amount"] = %obj.openAmount
  result["reminder_level"] = %obj.reminderLevel

