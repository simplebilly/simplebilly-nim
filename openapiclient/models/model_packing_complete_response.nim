#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type PackingCompleteResponse* = object
  ## 
  message*: string
  newState*: string
  orderNumber*: string
  success*: bool


# Custom JSON deserialization for PackingCompleteResponse with custom field names
proc to*(node: JsonNode, T: typedesc[PackingCompleteResponse]): PackingCompleteResponse =
  result = PackingCompleteResponse()
  if node.kind == JObject:
    if node.hasKey("message"):
      result.message = to(node["message"], string)
    if node.hasKey("new_state"):
      result.newState = to(node["new_state"], string)
    if node.hasKey("order_number"):
      result.orderNumber = to(node["order_number"], string)
    if node.hasKey("success"):
      result.success = to(node["success"], bool)

# Custom JSON serialization for PackingCompleteResponse with custom field names
proc `%`*(obj: PackingCompleteResponse): JsonNode =
  result = newJObject()
  result["message"] = %obj.message
  result["new_state"] = %obj.newState
  result["order_number"] = %obj.orderNumber
  result["success"] = %obj.success

