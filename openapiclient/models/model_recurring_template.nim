#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type

type RecurringTemplate* = object
  ## 
  createdAt*: string
  deletedAt*: string
  endDate*: Option[string]
  executionInterval*: string
  executionStatus*: string
  finalize*: bool
  lastExecutedAt*: Option[string]
  name*: string
  nextExecutionAt*: Option[string]
  startDate*: string
  templateId*: string
  templateType*: string
  updatedAt*: string
  voucherData*: Option[JsonNode]


# Custom JSON deserialization for RecurringTemplate with custom field names
proc to*(node: JsonNode, T: typedesc[RecurringTemplate]): RecurringTemplate =
  result = RecurringTemplate()
  if node.kind == JObject:
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], string)
    if node.hasKey("deleted_at"):
      result.deletedAt = to(node["deleted_at"], string)
    if node.hasKey("end_date") and node["end_date"].kind != JNull:
      result.endDate = some(to(node["end_date"], typeof(result.endDate.get())))
    if node.hasKey("execution_interval"):
      result.executionInterval = to(node["execution_interval"], string)
    if node.hasKey("execution_status"):
      result.executionStatus = to(node["execution_status"], string)
    if node.hasKey("finalize"):
      result.finalize = to(node["finalize"], bool)
    if node.hasKey("last_executed_at") and node["last_executed_at"].kind != JNull:
      result.lastExecutedAt = some(to(node["last_executed_at"], typeof(result.lastExecutedAt.get())))
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("next_execution_at") and node["next_execution_at"].kind != JNull:
      result.nextExecutionAt = some(to(node["next_execution_at"], typeof(result.nextExecutionAt.get())))
    if node.hasKey("start_date"):
      result.startDate = to(node["start_date"], string)
    if node.hasKey("template_id"):
      result.templateId = to(node["template_id"], string)
    if node.hasKey("template_type"):
      result.templateType = to(node["template_type"], string)
    if node.hasKey("updated_at"):
      result.updatedAt = to(node["updated_at"], string)
    if node.hasKey("voucher_data") and node["voucher_data"].kind != JNull:
      result.voucherData = some(to(node["voucher_data"], typeof(result.voucherData.get())))

# Custom JSON serialization for RecurringTemplate with custom field names
proc `%`*(obj: RecurringTemplate): JsonNode =
  result = newJObject()
  result["created_at"] = %obj.createdAt
  result["deleted_at"] = %obj.deletedAt
  if obj.endDate.isSome():
    result["end_date"] = %obj.endDate.get()
  result["execution_interval"] = %obj.executionInterval
  result["execution_status"] = %obj.executionStatus
  result["finalize"] = %obj.finalize
  if obj.lastExecutedAt.isSome():
    result["last_executed_at"] = %obj.lastExecutedAt.get()
  result["name"] = %obj.name
  if obj.nextExecutionAt.isSome():
    result["next_execution_at"] = %obj.nextExecutionAt.get()
  result["start_date"] = %obj.startDate
  result["template_id"] = %obj.templateId
  result["template_type"] = %obj.templateType
  result["updated_at"] = %obj.updatedAt
  if obj.voucherData.isSome():
    result["voucher_data"] = %obj.voucherData.get()

