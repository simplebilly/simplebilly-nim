#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_category_total
import model_data_quality
import model_scope_total
import model_target_progress
import model_year_total

type EmissionsReport* = object
  ## 
  byCategory*: seq[CategoryTotal]
  byScope*: seq[ScopeTotal]
  byYear*: seq[YearTotal]
  dataQuality*: DataQuality
  intensityPerEmployee*: Option[float64]
  intensityPerRevenueMio*: Option[float64] ## tCO2e per million EUR net revenue.
  netRevenue*: Option[float64] ## Sum of paid/sent/partially-paid invoices (EUR net) in the year.
  spendBasedEstimateTco2e*: Option[float64] ## Spend-based estimate from bookkeeping payments (EXIOBASE factor).
  targets*: seq[TargetProgress]
  totalTco2e*: string


# Custom JSON deserialization for EmissionsReport with custom field names
proc to*(node: JsonNode, T: typedesc[EmissionsReport]): EmissionsReport =
  result = EmissionsReport()
  if node.kind == JObject:
    if node.hasKey("by_category"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["by_category"]
      if arrayNode.kind == JArray:
        result.byCategory = @[]
        for item in arrayNode.items:
          result.byCategory.add(to(item, CategoryTotal))
    if node.hasKey("by_scope"):
      result.byScope = to(node["by_scope"], seq[ScopeTotal])
    if node.hasKey("by_year"):
      result.byYear = to(node["by_year"], seq[YearTotal])
    if node.hasKey("data_quality"):
      result.dataQuality = to(node["data_quality"], DataQuality)
    if node.hasKey("intensity_per_employee") and node["intensity_per_employee"].kind != JNull:
      result.intensityPerEmployee = some(to(node["intensity_per_employee"], typeof(result.intensityPerEmployee.get())))
    if node.hasKey("intensity_per_revenue_mio") and node["intensity_per_revenue_mio"].kind != JNull:
      result.intensityPerRevenueMio = some(to(node["intensity_per_revenue_mio"], typeof(result.intensityPerRevenueMio.get())))
    if node.hasKey("net_revenue") and node["net_revenue"].kind != JNull:
      result.netRevenue = some(to(node["net_revenue"], typeof(result.netRevenue.get())))
    if node.hasKey("spend_based_estimate_tco2e") and node["spend_based_estimate_tco2e"].kind != JNull:
      result.spendBasedEstimateTco2e = some(to(node["spend_based_estimate_tco2e"], typeof(result.spendBasedEstimateTco2e.get())))
    if node.hasKey("targets"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["targets"]
      if arrayNode.kind == JArray:
        result.targets = @[]
        for item in arrayNode.items:
          result.targets.add(to(item, TargetProgress))
    if node.hasKey("total_tco2e"):
      result.totalTco2e = to(node["total_tco2e"], string)

# Custom JSON serialization for EmissionsReport with custom field names
proc `%`*(obj: EmissionsReport): JsonNode =
  result = newJObject()
  result["by_category"] = %obj.byCategory
  result["by_scope"] = %obj.byScope
  result["by_year"] = %obj.byYear
  result["data_quality"] = %obj.dataQuality
  if obj.intensityPerEmployee.isSome():
    result["intensity_per_employee"] = %obj.intensityPerEmployee.get()
  if obj.intensityPerRevenueMio.isSome():
    result["intensity_per_revenue_mio"] = %obj.intensityPerRevenueMio.get()
  if obj.netRevenue.isSome():
    result["net_revenue"] = %obj.netRevenue.get()
  if obj.spendBasedEstimateTco2e.isSome():
    result["spend_based_estimate_tco2e"] = %obj.spendBasedEstimateTco2e.get()
  result["targets"] = %obj.targets
  result["total_tco2e"] = %obj.totalTco2e

