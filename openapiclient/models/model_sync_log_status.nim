#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type SyncLogStatus* {.pure.} = enum
  Running
  Success
  Error

func `%`*(v: SyncLogStatus): JsonNode =
  result = case v:
    of SyncLogStatus.Running: %"running"
    of SyncLogStatus.Success: %"success"
    of SyncLogStatus.Error: %"error"

func `$`*(v: SyncLogStatus): string =
  result = case v:
    of SyncLogStatus.Running: $("running")
    of SyncLogStatus.Success: $("success")
    of SyncLogStatus.Error: $("error")

proc to*(node: JsonNode, T: typedesc[SyncLogStatus]): SyncLogStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum SyncLogStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("running"):
    return SyncLogStatus.Running
  of $("success"):
    return SyncLogStatus.Success
  of $("error"):
    return SyncLogStatus.Error
  else:
    raise newException(ValueError, "Invalid enum value for SyncLogStatus: " & strVal)

