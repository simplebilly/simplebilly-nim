#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type LegalDocType* {.pure.} = enum
  Imprint
  Privacy
  Terms
  Withdrawal
  Refund
  Shipping
  Gpsr
  CookieNotice

func `%`*(v: LegalDocType): JsonNode =
  result = case v:
    of LegalDocType.Imprint: %"imprint"
    of LegalDocType.Privacy: %"privacy"
    of LegalDocType.Terms: %"terms"
    of LegalDocType.Withdrawal: %"withdrawal"
    of LegalDocType.Refund: %"refund"
    of LegalDocType.Shipping: %"shipping"
    of LegalDocType.Gpsr: %"gpsr"
    of LegalDocType.CookieNotice: %"cookie_notice"

func `$`*(v: LegalDocType): string =
  result = case v:
    of LegalDocType.Imprint: $("imprint")
    of LegalDocType.Privacy: $("privacy")
    of LegalDocType.Terms: $("terms")
    of LegalDocType.Withdrawal: $("withdrawal")
    of LegalDocType.Refund: $("refund")
    of LegalDocType.Shipping: $("shipping")
    of LegalDocType.Gpsr: $("gpsr")
    of LegalDocType.CookieNotice: $("cookie_notice")

proc to*(node: JsonNode, T: typedesc[LegalDocType]): LegalDocType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum LegalDocType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("imprint"):
    return LegalDocType.Imprint
  of $("privacy"):
    return LegalDocType.Privacy
  of $("terms"):
    return LegalDocType.Terms
  of $("withdrawal"):
    return LegalDocType.Withdrawal
  of $("refund"):
    return LegalDocType.Refund
  of $("shipping"):
    return LegalDocType.Shipping
  of $("gpsr"):
    return LegalDocType.Gpsr
  of $("cookie_notice"):
    return LegalDocType.CookieNotice
  else:
    raise newException(ValueError, "Invalid enum value for LegalDocType: " & strVal)

