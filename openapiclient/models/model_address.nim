#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Address* = object
  ## 
  city*: string
  company*: Option[string]
  country*: string ## ISO 3166-1 alpha-2 country code (e.g. \"DE\", \"PL\", \"FR\").
  email*: Option[string]
  name*: string
  phone*: Option[string]
  street*: string
  streetNumber*: string
  zip*: string


# Custom JSON deserialization for Address with custom field names
proc to*(node: JsonNode, T: typedesc[Address]): Address =
  result = Address()
  if node.kind == JObject:
    if node.hasKey("city"):
      result.city = to(node["city"], string)
    if node.hasKey("company") and node["company"].kind != JNull:
      result.company = some(to(node["company"], typeof(result.company.get())))
    if node.hasKey("country"):
      result.country = to(node["country"], string)
    if node.hasKey("email") and node["email"].kind != JNull:
      result.email = some(to(node["email"], typeof(result.email.get())))
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("phone") and node["phone"].kind != JNull:
      result.phone = some(to(node["phone"], typeof(result.phone.get())))
    if node.hasKey("street"):
      result.street = to(node["street"], string)
    if node.hasKey("street_number"):
      result.streetNumber = to(node["street_number"], string)
    if node.hasKey("zip"):
      result.zip = to(node["zip"], string)

# Custom JSON serialization for Address with custom field names
proc `%`*(obj: Address): JsonNode =
  result = newJObject()
  result["city"] = %obj.city
  if obj.company.isSome():
    result["company"] = %obj.company.get()
  result["country"] = %obj.country
  if obj.email.isSome():
    result["email"] = %obj.email.get()
  result["name"] = %obj.name
  if obj.phone.isSome():
    result["phone"] = %obj.phone.get()
  result["street"] = %obj.street
  result["street_number"] = %obj.streetNumber
  result["zip"] = %obj.zip

