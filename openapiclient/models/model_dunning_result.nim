#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type DunningResult* = object
  ## 
  invoicesProcessed*: int
  message*: string


# Custom JSON deserialization for DunningResult with custom field names
proc to*(node: JsonNode, T: typedesc[DunningResult]): DunningResult =
  result = DunningResult()
  if node.kind == JObject:
    if node.hasKey("invoices_processed"):
      result.invoicesProcessed = to(node["invoices_processed"], int)
    if node.hasKey("message"):
      result.message = to(node["message"], string)

# Custom JSON serialization for DunningResult with custom field names
proc `%`*(obj: DunningResult): JsonNode =
  result = newJObject()
  result["invoices_processed"] = %obj.invoicesProcessed
  result["message"] = %obj.message

