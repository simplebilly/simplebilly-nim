#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type GewerbesteuerErgebnis* = object
  ## 
  freibetrag*: string
  gesamtbelastung*: string
  gewerbeertrag*: string
  hebesatz*: string
  jahr*: int
  koerperschaftsteuer*: string
  land*: string
  messbetrag*: string
  steuer*: string
  steuerArt*: string


# Custom JSON deserialization for GewerbesteuerErgebnis with custom field names
proc to*(node: JsonNode, T: typedesc[GewerbesteuerErgebnis]): GewerbesteuerErgebnis =
  result = GewerbesteuerErgebnis()
  if node.kind == JObject:
    if node.hasKey("freibetrag"):
      result.freibetrag = to(node["freibetrag"], string)
    if node.hasKey("gesamtbelastung"):
      result.gesamtbelastung = to(node["gesamtbelastung"], string)
    if node.hasKey("gewerbeertrag"):
      result.gewerbeertrag = to(node["gewerbeertrag"], string)
    if node.hasKey("hebesatz"):
      result.hebesatz = to(node["hebesatz"], string)
    if node.hasKey("jahr"):
      result.jahr = to(node["jahr"], int)
    if node.hasKey("koerperschaftsteuer"):
      result.koerperschaftsteuer = to(node["koerperschaftsteuer"], string)
    if node.hasKey("land"):
      result.land = to(node["land"], string)
    if node.hasKey("messbetrag"):
      result.messbetrag = to(node["messbetrag"], string)
    if node.hasKey("steuer"):
      result.steuer = to(node["steuer"], string)
    if node.hasKey("steuer_art"):
      result.steuerArt = to(node["steuer_art"], string)

# Custom JSON serialization for GewerbesteuerErgebnis with custom field names
proc `%`*(obj: GewerbesteuerErgebnis): JsonNode =
  result = newJObject()
  result["freibetrag"] = %obj.freibetrag
  result["gesamtbelastung"] = %obj.gesamtbelastung
  result["gewerbeertrag"] = %obj.gewerbeertrag
  result["hebesatz"] = %obj.hebesatz
  result["jahr"] = %obj.jahr
  result["koerperschaftsteuer"] = %obj.koerperschaftsteuer
  result["land"] = %obj.land
  result["messbetrag"] = %obj.messbetrag
  result["steuer"] = %obj.steuer
  result["steuer_art"] = %obj.steuerArt

