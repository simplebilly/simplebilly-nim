#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_activity_status
import model_activity_type

type ActivityUpdate* = object
  ## 
  activityType*: Option[ActivityType] ## One of: call | email | meeting | task | note
  assignedTo*: Option[string] ## User responsible (`employee.employee_id`).
  contactId*: Option[string] ## Contact this activity belongs to (`contact.contact_id`). References the contact entity.
  description*: Option[string]
  dueDate*: Option[string] ## Follow-up / Wiedervorlage date. Open activities with a due date in the past are overdue.
  reminderDate*: Option[string] ## When to remind about the follow-up.
  status*: Option[ActivityStatus] ## One of: open | done | cancelled
  subject*: Option[string] ## Short subject line.

