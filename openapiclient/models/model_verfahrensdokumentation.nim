#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_compliance_entry

type Verfahrensdokumentation* = object
  ## 
  entries*: seq[ComplianceEntry]
  generatedAt*: string
  title*: string
  version*: string


# Custom JSON deserialization for Verfahrensdokumentation with custom field names
proc to*(node: JsonNode, T: typedesc[Verfahrensdokumentation]): Verfahrensdokumentation =
  result = Verfahrensdokumentation()
  if node.kind == JObject:
    if node.hasKey("entries"):
      result.entries = to(node["entries"], seq[ComplianceEntry])
    if node.hasKey("generated_at"):
      result.generatedAt = to(node["generated_at"], string)
    if node.hasKey("title"):
      result.title = to(node["title"], string)
    if node.hasKey("version"):
      result.version = to(node["version"], string)

# Custom JSON serialization for Verfahrensdokumentation with custom field names
proc `%`*(obj: Verfahrensdokumentation): JsonNode =
  result = newJObject()
  result["entries"] = %obj.entries
  result["generated_at"] = %obj.generatedAt
  result["title"] = %obj.title
  result["version"] = %obj.version

