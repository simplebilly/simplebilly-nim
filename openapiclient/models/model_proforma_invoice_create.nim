#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type
import model_currency_code
import model_proforma_invoice_status

type ProformaInvoiceCreate* = object
  ## 
  convertedAt*: Option[string]
  convertedToInvoiceId*: Option[string] ## Set when the proforma was converted into a real invoice. References the invoice entity.
  currency*: CurrencyCode
  customerId*: Option[string] ## References the customer entity.
  customerSnapshot*: Option[JsonNode] ## Snapshot of the recipient at issue time (address, VAT id, …).
  issueDate*: string
  lineItems*: Option[JsonNode]
  notes*: Option[string]
  orderNumber*: Option[string] ## Reference to the order/quote this proforma belongs to.
  paymentDueDate*: Option[string] ## Optional deadline the real invoice should carry after conversion.
  quotationId*: Option[string] ## References the quotation entity.
  status*: ProformaInvoiceStatus ## `draft` | `sent` | `converted`.
  subtotal*: string
  totalAmount*: string
  totalTax*: string

