#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type GhgScope* {.pure.} = enum
  `1`
  `2`
  `3`

func `%`*(v: GhgScope): JsonNode =
  result = case v:
    of GhgScope.`1`: %"1"
    of GhgScope.`2`: %"2"
    of GhgScope.`3`: %"3"

func `$`*(v: GhgScope): string =
  result = case v:
    of GhgScope.`1`: $("1")
    of GhgScope.`2`: $("2")
    of GhgScope.`3`: $("3")

proc to*(node: JsonNode, T: typedesc[GhgScope]): GhgScope =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum GhgScope, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("1"):
    return GhgScope.`1`
  of $("2"):
    return GhgScope.`2`
  of $("3"):
    return GhgScope.`3`
  else:
    raise newException(ValueError, "Invalid enum value for GhgScope: " & strVal)

