#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type StillePartnerZeile* = object
  ## 
  auseinandersetzungsguthaben*: string
  gewinnanteil*: string
  gewinnvortrag*: string
  hinweis*: Option[string]
  instrumentType*: string
  kest*: string
  name*: string
  verlustVerrechnungskonto*: string
  verlustanteil*: string


# Custom JSON deserialization for StillePartnerZeile with custom field names
proc to*(node: JsonNode, T: typedesc[StillePartnerZeile]): StillePartnerZeile =
  result = StillePartnerZeile()
  if node.kind == JObject:
    if node.hasKey("auseinandersetzungsguthaben"):
      result.auseinandersetzungsguthaben = to(node["auseinandersetzungsguthaben"], string)
    if node.hasKey("gewinnanteil"):
      result.gewinnanteil = to(node["gewinnanteil"], string)
    if node.hasKey("gewinnvortrag"):
      result.gewinnvortrag = to(node["gewinnvortrag"], string)
    if node.hasKey("hinweis") and node["hinweis"].kind != JNull:
      result.hinweis = some(to(node["hinweis"], typeof(result.hinweis.get())))
    if node.hasKey("instrument_type"):
      result.instrumentType = to(node["instrument_type"], string)
    if node.hasKey("kest"):
      result.kest = to(node["kest"], string)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("verlust_verrechnungskonto"):
      result.verlustVerrechnungskonto = to(node["verlust_verrechnungskonto"], string)
    if node.hasKey("verlustanteil"):
      result.verlustanteil = to(node["verlustanteil"], string)

# Custom JSON serialization for StillePartnerZeile with custom field names
proc `%`*(obj: StillePartnerZeile): JsonNode =
  result = newJObject()
  result["auseinandersetzungsguthaben"] = %obj.auseinandersetzungsguthaben
  result["gewinnanteil"] = %obj.gewinnanteil
  result["gewinnvortrag"] = %obj.gewinnvortrag
  if obj.hinweis.isSome():
    result["hinweis"] = %obj.hinweis.get()
  result["instrument_type"] = %obj.instrumentType
  result["kest"] = %obj.kest
  result["name"] = %obj.name
  result["verlust_verrechnungskonto"] = %obj.verlustVerrechnungskonto
  result["verlustanteil"] = %obj.verlustanteil

