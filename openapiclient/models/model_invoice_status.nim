#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type InvoiceStatus* {.pure.} = enum
  Draft
  Sent
  PartiallyPaid
  Paid
  Overdue
  Cancelled
  Credited
  Expired
  Dunning
  CollectDebt

func `%`*(v: InvoiceStatus): JsonNode =
  result = case v:
    of InvoiceStatus.Draft: %"Draft"
    of InvoiceStatus.Sent: %"Sent"
    of InvoiceStatus.PartiallyPaid: %"PartiallyPaid"
    of InvoiceStatus.Paid: %"Paid"
    of InvoiceStatus.Overdue: %"Overdue"
    of InvoiceStatus.Cancelled: %"Cancelled"
    of InvoiceStatus.Credited: %"Credited"
    of InvoiceStatus.Expired: %"Expired"
    of InvoiceStatus.Dunning: %"Dunning"
    of InvoiceStatus.CollectDebt: %"CollectDebt"

func `$`*(v: InvoiceStatus): string =
  result = case v:
    of InvoiceStatus.Draft: $("Draft")
    of InvoiceStatus.Sent: $("Sent")
    of InvoiceStatus.PartiallyPaid: $("PartiallyPaid")
    of InvoiceStatus.Paid: $("Paid")
    of InvoiceStatus.Overdue: $("Overdue")
    of InvoiceStatus.Cancelled: $("Cancelled")
    of InvoiceStatus.Credited: $("Credited")
    of InvoiceStatus.Expired: $("Expired")
    of InvoiceStatus.Dunning: $("Dunning")
    of InvoiceStatus.CollectDebt: $("CollectDebt")

proc to*(node: JsonNode, T: typedesc[InvoiceStatus]): InvoiceStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum InvoiceStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("Draft"):
    return InvoiceStatus.Draft
  of $("Sent"):
    return InvoiceStatus.Sent
  of $("PartiallyPaid"):
    return InvoiceStatus.PartiallyPaid
  of $("Paid"):
    return InvoiceStatus.Paid
  of $("Overdue"):
    return InvoiceStatus.Overdue
  of $("Cancelled"):
    return InvoiceStatus.Cancelled
  of $("Credited"):
    return InvoiceStatus.Credited
  of $("Expired"):
    return InvoiceStatus.Expired
  of $("Dunning"):
    return InvoiceStatus.Dunning
  of $("CollectDebt"):
    return InvoiceStatus.CollectDebt
  else:
    raise newException(ValueError, "Invalid enum value for InvoiceStatus: " & strVal)

