#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type KonzernBeteiligung* = object
  ## 
  companyName*: string
  controlBasis*: seq[string] ## Erfüllte Kontroll-Indikatoren (§ 290 Abs. 2 HGB) als deutsche Bezeichnungen.
  controlled*: bool
  ownershipPct*: string


# Custom JSON deserialization for KonzernBeteiligung with custom field names
proc to*(node: JsonNode, T: typedesc[KonzernBeteiligung]): KonzernBeteiligung =
  result = KonzernBeteiligung()
  if node.kind == JObject:
    if node.hasKey("company_name"):
      result.companyName = to(node["company_name"], string)
    if node.hasKey("control_basis"):
      result.controlBasis = to(node["control_basis"], seq[string])
    if node.hasKey("controlled"):
      result.controlled = to(node["controlled"], bool)
    if node.hasKey("ownership_pct"):
      result.ownershipPct = to(node["ownership_pct"], string)

# Custom JSON serialization for KonzernBeteiligung with custom field names
proc `%`*(obj: KonzernBeteiligung): JsonNode =
  result = newJObject()
  result["company_name"] = %obj.companyName
  result["control_basis"] = %obj.controlBasis
  result["controlled"] = %obj.controlled
  result["ownership_pct"] = %obj.ownershipPct

