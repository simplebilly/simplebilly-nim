#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_address
import model_cart_item_input

type SuitabilityRequest* = object
  ## 
  customerAnnualVolume*: Option[int]
  items*: seq[CartItemInput]
  recipient*: Address
  sender*: Address


# Custom JSON deserialization for SuitabilityRequest with custom field names
proc to*(node: JsonNode, T: typedesc[SuitabilityRequest]): SuitabilityRequest =
  result = SuitabilityRequest()
  if node.kind == JObject:
    if node.hasKey("customer_annual_volume") and node["customer_annual_volume"].kind != JNull:
      result.customerAnnualVolume = some(to(node["customer_annual_volume"], typeof(result.customerAnnualVolume.get())))
    if node.hasKey("items"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["items"]
      if arrayNode.kind == JArray:
        result.items = @[]
        for item in arrayNode.items:
          result.items.add(to(item, CartItemInput))
    if node.hasKey("recipient"):
      result.recipient = to(node["recipient"], Address)
    if node.hasKey("sender"):
      result.sender = to(node["sender"], Address)

# Custom JSON serialization for SuitabilityRequest with custom field names
proc `%`*(obj: SuitabilityRequest): JsonNode =
  result = newJObject()
  if obj.customerAnnualVolume.isSome():
    result["customer_annual_volume"] = %obj.customerAnnualVolume.get()
  result["items"] = %obj.items
  result["recipient"] = %obj.recipient
  result["sender"] = %obj.sender

