#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_extra_payment

type PayrollCreatePayload* = object
  ## 
  employeeIds*: seq[string]
  extraPayments*: Option[seq[ExtraPayment]]
  month*: int
  year*: int


# Custom JSON deserialization for PayrollCreatePayload with custom field names
proc to*(node: JsonNode, T: typedesc[PayrollCreatePayload]): PayrollCreatePayload =
  result = PayrollCreatePayload()
  if node.kind == JObject:
    if node.hasKey("employee_ids"):
      result.employeeIds = to(node["employee_ids"], seq[string])
    if node.hasKey("extra_payments") and node["extra_payments"].kind != JNull:
      # Optional array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["extra_payments"]
      if arrayNode.kind == JArray:
        var arr: seq[ExtraPayment] = @[]
        for item in arrayNode.items:
          arr.add(to(item, ExtraPayment))
        result.extraPayments = some(arr)
    if node.hasKey("month"):
      result.month = to(node["month"], int)
    if node.hasKey("year"):
      result.year = to(node["year"], int)

# Custom JSON serialization for PayrollCreatePayload with custom field names
proc `%`*(obj: PayrollCreatePayload): JsonNode =
  result = newJObject()
  result["employee_ids"] = %obj.employeeIds
  if obj.extraPayments.isSome():
    result["extra_payments"] = %obj.extraPayments.get()
  result["month"] = %obj.month
  result["year"] = %obj.year

