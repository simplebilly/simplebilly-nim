#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type HebesatzLookup* = object
  ## 
  bundesland*: string
  countryCode*: string
  gemeindeName*: string
  gemeindeschluessel*: string
  hebesatzGewerbesteuer*: float64
  hebesatzGrundsteuerB*: Option[float64]
  jahr*: int
  landkreis*: Option[string]
  validFrom*: string
  validTo*: Option[string]


# Custom JSON deserialization for HebesatzLookup with custom field names
proc to*(node: JsonNode, T: typedesc[HebesatzLookup]): HebesatzLookup =
  result = HebesatzLookup()
  if node.kind == JObject:
    if node.hasKey("bundesland"):
      result.bundesland = to(node["bundesland"], string)
    if node.hasKey("country_code"):
      result.countryCode = to(node["country_code"], string)
    if node.hasKey("gemeinde_name"):
      result.gemeindeName = to(node["gemeinde_name"], string)
    if node.hasKey("gemeindeschluessel"):
      result.gemeindeschluessel = to(node["gemeindeschluessel"], string)
    if node.hasKey("hebesatz_gewerbesteuer"):
      result.hebesatzGewerbesteuer = to(node["hebesatz_gewerbesteuer"], float64)
    if node.hasKey("hebesatz_grundsteuer_b") and node["hebesatz_grundsteuer_b"].kind != JNull:
      result.hebesatzGrundsteuerB = some(to(node["hebesatz_grundsteuer_b"], typeof(result.hebesatzGrundsteuerB.get())))
    if node.hasKey("jahr"):
      result.jahr = to(node["jahr"], int)
    if node.hasKey("landkreis") and node["landkreis"].kind != JNull:
      result.landkreis = some(to(node["landkreis"], typeof(result.landkreis.get())))
    if node.hasKey("valid_from"):
      result.validFrom = to(node["valid_from"], string)
    if node.hasKey("valid_to") and node["valid_to"].kind != JNull:
      result.validTo = some(to(node["valid_to"], typeof(result.validTo.get())))

# Custom JSON serialization for HebesatzLookup with custom field names
proc `%`*(obj: HebesatzLookup): JsonNode =
  result = newJObject()
  result["bundesland"] = %obj.bundesland
  result["country_code"] = %obj.countryCode
  result["gemeinde_name"] = %obj.gemeindeName
  result["gemeindeschluessel"] = %obj.gemeindeschluessel
  result["hebesatz_gewerbesteuer"] = %obj.hebesatzGewerbesteuer
  if obj.hebesatzGrundsteuerB.isSome():
    result["hebesatz_grundsteuer_b"] = %obj.hebesatzGrundsteuerB.get()
  result["jahr"] = %obj.jahr
  if obj.landkreis.isSome():
    result["landkreis"] = %obj.landkreis.get()
  result["valid_from"] = %obj.validFrom
  if obj.validTo.isSome():
    result["valid_to"] = %obj.validTo.get()

