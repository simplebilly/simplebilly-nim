#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_service_job_status

type ServiceJob* = object
  ## 
  address*: Option[string] ## Street + zip + city of the job location.
  customerEmail*: Option[string] ## Customer email for email notifications.
  customerId*: Option[string] ## References the customer entity.
  customerName*: Option[string] ## Denormalized customer name for quick display.
  customerPhone*: Option[string] ## Customer phone for SMS notifications later.
  description*: Option[string] ## What work needs to be done.
  estimatedDurationMinutes*: Option[int] ## Estimated time for the job in minutes.
  lat*: Option[float64] ## Latitude for map display (OpenStreetMap).
  lng*: Option[float64] ## Longitude for map display (OpenStreetMap).
  notes*: Option[string]
  status*: Option[ServiceJobStatus] ## Dispatch status: \"pending\", \"assigned\", \"en_route\", \"in_progress\", \"completed\", \"cancelled\".

