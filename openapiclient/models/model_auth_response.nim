#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_model

type AuthResponse* = object
  ## 
  accessToken*: Option[string]
  message*: Option[string]
  refreshToken*: Option[string]
  success*: bool
  user*: Option[Model]


# Custom JSON deserialization for AuthResponse with custom field names
proc to*(node: JsonNode, T: typedesc[AuthResponse]): AuthResponse =
  result = AuthResponse()
  if node.kind == JObject:
    if node.hasKey("access_token") and node["access_token"].kind != JNull:
      result.accessToken = some(to(node["access_token"], typeof(result.accessToken.get())))
    if node.hasKey("message") and node["message"].kind != JNull:
      result.message = some(to(node["message"], typeof(result.message.get())))
    if node.hasKey("refresh_token") and node["refresh_token"].kind != JNull:
      result.refreshToken = some(to(node["refresh_token"], typeof(result.refreshToken.get())))
    if node.hasKey("success"):
      result.success = to(node["success"], bool)
    if node.hasKey("user") and node["user"].kind != JNull:
      result.user = some(to(node["user"], typeof(result.user.get())))

# Custom JSON serialization for AuthResponse with custom field names
proc `%`*(obj: AuthResponse): JsonNode =
  result = newJObject()
  if obj.accessToken.isSome():
    result["access_token"] = %obj.accessToken.get()
  if obj.message.isSome():
    result["message"] = %obj.message.get()
  if obj.refreshToken.isSome():
    result["refresh_token"] = %obj.refreshToken.get()
  result["success"] = %obj.success
  if obj.user.isSome():
    result["user"] = %obj.user.get()

