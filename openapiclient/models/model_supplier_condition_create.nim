#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type

type SupplierConditionCreate* = object
  ## 
  currency*: string ## Currency for the minimum order value.
  deliveryTerms*: Option[string] ## Incoterms, e.g. \"EXW\", \"DAP\".
  earlyPaymentDiscountPercent*: Option[string] ## Early-payment discount percentage (Skonto), e.g. 2.0.
  isDefault*: Option[bool] ## Is this the default condition for the supplier?
  minimumOrderValue*: Option[string] ## Minimum order value required for this supplier.
  notes*: Option[string]
  paymentDueDays*: Option[int] ## Number of days within which payment is due.
  paymentTerms*: Option[string] ## Payment terms, e.g. \"14 Tage, 2% Skonto\".
  supplierContactId*: string ## The supplier this condition applies to (`contact_id`). References the supplier entity.
  supplierName*: Option[string] ## The name of the supplier, denormalized for easy listing.
  volumeDiscountTiers*: Option[JsonNode] ## Tiered discounts: JSON array of `{min_quantity, discount_percent}`.

