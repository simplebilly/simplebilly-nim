#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type InventoryValuePoint* = object
  ## 
  productCount*: int64
  recordedAt*: string
  totalPurchaseValue*: string
  totalSalesValue*: string


# Custom JSON deserialization for InventoryValuePoint with custom field names
proc to*(node: JsonNode, T: typedesc[InventoryValuePoint]): InventoryValuePoint =
  result = InventoryValuePoint()
  if node.kind == JObject:
    if node.hasKey("product_count"):
      result.productCount = to(node["product_count"], int64)
    if node.hasKey("recorded_at"):
      result.recordedAt = to(node["recorded_at"], string)
    if node.hasKey("total_purchase_value"):
      result.totalPurchaseValue = to(node["total_purchase_value"], string)
    if node.hasKey("total_sales_value"):
      result.totalSalesValue = to(node["total_sales_value"], string)

# Custom JSON serialization for InventoryValuePoint with custom field names
proc `%`*(obj: InventoryValuePoint): JsonNode =
  result = newJObject()
  result["product_count"] = %obj.productCount
  result["recorded_at"] = %obj.recordedAt
  result["total_purchase_value"] = %obj.totalPurchaseValue
  result["total_sales_value"] = %obj.totalSalesValue

