#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_payroll_run_status

type PayrollSummaryItem* = object
  ## 
  employeeCount*: int
  month*: string
  status*: PayrollRunStatus
  totalEmployerCost*: string
  totalGross*: string
  totalNet*: string
  year*: int


# Custom JSON deserialization for PayrollSummaryItem with custom field names
proc to*(node: JsonNode, T: typedesc[PayrollSummaryItem]): PayrollSummaryItem =
  result = PayrollSummaryItem()
  if node.kind == JObject:
    if node.hasKey("employee_count"):
      result.employeeCount = to(node["employee_count"], int)
    if node.hasKey("month"):
      result.month = to(node["month"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], PayrollRunStatus)
    if node.hasKey("total_employer_cost"):
      result.totalEmployerCost = to(node["total_employer_cost"], string)
    if node.hasKey("total_gross"):
      result.totalGross = to(node["total_gross"], string)
    if node.hasKey("total_net"):
      result.totalNet = to(node["total_net"], string)
    if node.hasKey("year"):
      result.year = to(node["year"], int)

# Custom JSON serialization for PayrollSummaryItem with custom field names
proc `%`*(obj: PayrollSummaryItem): JsonNode =
  result = newJObject()
  result["employee_count"] = %obj.employeeCount
  result["month"] = %obj.month
  result["status"] = %obj.status
  result["total_employer_cost"] = %obj.totalEmployerCost
  result["total_gross"] = %obj.totalGross
  result["total_net"] = %obj.totalNet
  result["year"] = %obj.year

