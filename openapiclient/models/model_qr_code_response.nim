#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type QRCodeResponse* = object
  ## 
  contentType*: string
  qrCodeBase64*: string


# Custom JSON deserialization for QRCodeResponse with custom field names
proc to*(node: JsonNode, T: typedesc[QRCodeResponse]): QRCodeResponse =
  result = QRCodeResponse()
  if node.kind == JObject:
    if node.hasKey("content_type"):
      result.contentType = to(node["content_type"], string)
    if node.hasKey("qr_code_base64"):
      result.qrCodeBase64 = to(node["qr_code_base64"], string)

# Custom JSON serialization for QRCodeResponse with custom field names
proc `%`*(obj: QRCodeResponse): JsonNode =
  result = newJObject()
  result["content_type"] = %obj.contentType
  result["qr_code_base64"] = %obj.qrCodeBase64

