#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type WebhookEventStatus* {.pure.} = enum
  Accepted
  Delivered
  Failed
  Received

func `%`*(v: WebhookEventStatus): JsonNode =
  result = case v:
    of WebhookEventStatus.Accepted: %"accepted"
    of WebhookEventStatus.Delivered: %"delivered"
    of WebhookEventStatus.Failed: %"failed"
    of WebhookEventStatus.Received: %"received"

func `$`*(v: WebhookEventStatus): string =
  result = case v:
    of WebhookEventStatus.Accepted: $("accepted")
    of WebhookEventStatus.Delivered: $("delivered")
    of WebhookEventStatus.Failed: $("failed")
    of WebhookEventStatus.Received: $("received")

proc to*(node: JsonNode, T: typedesc[WebhookEventStatus]): WebhookEventStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum WebhookEventStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("accepted"):
    return WebhookEventStatus.Accepted
  of $("delivered"):
    return WebhookEventStatus.Delivered
  of $("failed"):
    return WebhookEventStatus.Failed
  of $("received"):
    return WebhookEventStatus.Received
  else:
    raise newException(ValueError, "Invalid enum value for WebhookEventStatus: " & strVal)

