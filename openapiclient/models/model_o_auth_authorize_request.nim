#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type

type OAuthAuthorizeRequest* = object
  ## 
  config*: Option[JsonNode] ## Optional platform-specific config (e.g. Shopify `shop_domain`, `api_key`, `api_secret`) needed to build the authorization URL.
  platform*: string
  redirectUri*: string


# Custom JSON deserialization for OAuthAuthorizeRequest with custom field names
proc to*(node: JsonNode, T: typedesc[OAuthAuthorizeRequest]): OAuthAuthorizeRequest =
  result = OAuthAuthorizeRequest()
  if node.kind == JObject:
    if node.hasKey("config") and node["config"].kind != JNull:
      result.config = some(to(node["config"], typeof(result.config.get())))
    if node.hasKey("platform"):
      result.platform = to(node["platform"], string)
    if node.hasKey("redirect_uri"):
      result.redirectUri = to(node["redirect_uri"], string)

# Custom JSON serialization for OAuthAuthorizeRequest with custom field names
proc `%`*(obj: OAuthAuthorizeRequest): JsonNode =
  result = newJObject()
  if obj.config.isSome():
    result["config"] = %obj.config.get()
  result["platform"] = %obj.platform
  result["redirect_uri"] = %obj.redirectUri

