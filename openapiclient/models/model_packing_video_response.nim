#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type PackingVideoResponse* = object
  ## 
  message*: string
  recordingUrl*: Option[string]
  success*: bool


# Custom JSON deserialization for PackingVideoResponse with custom field names
proc to*(node: JsonNode, T: typedesc[PackingVideoResponse]): PackingVideoResponse =
  result = PackingVideoResponse()
  if node.kind == JObject:
    if node.hasKey("message"):
      result.message = to(node["message"], string)
    if node.hasKey("recording_url") and node["recording_url"].kind != JNull:
      result.recordingUrl = some(to(node["recording_url"], typeof(result.recordingUrl.get())))
    if node.hasKey("success"):
      result.success = to(node["success"], bool)

# Custom JSON serialization for PackingVideoResponse with custom field names
proc `%`*(obj: PackingVideoResponse): JsonNode =
  result = newJObject()
  result["message"] = %obj.message
  if obj.recordingUrl.isSome():
    result["recording_url"] = %obj.recordingUrl.get()
  result["success"] = %obj.success

