#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ApiResponseTeamData* = object
  ## 
  createdAt*: string
  description*: Option[string]
  id*: string
  name*: string
  parentTeamId*: Option[string]
  tenantId*: string
  updatedAt*: string


# Custom JSON deserialization for ApiResponseTeamData with custom field names
proc to*(node: JsonNode, T: typedesc[ApiResponseTeamData]): ApiResponseTeamData =
  result = ApiResponseTeamData()
  if node.kind == JObject:
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], string)
    if node.hasKey("description") and node["description"].kind != JNull:
      result.description = some(to(node["description"], typeof(result.description.get())))
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("parent_team_id") and node["parent_team_id"].kind != JNull:
      result.parentTeamId = some(to(node["parent_team_id"], typeof(result.parentTeamId.get())))
    if node.hasKey("tenant_id"):
      result.tenantId = to(node["tenant_id"], string)
    if node.hasKey("updated_at"):
      result.updatedAt = to(node["updated_at"], string)

# Custom JSON serialization for ApiResponseTeamData with custom field names
proc `%`*(obj: ApiResponseTeamData): JsonNode =
  result = newJObject()
  result["created_at"] = %obj.createdAt
  if obj.description.isSome():
    result["description"] = %obj.description.get()
  result["id"] = %obj.id
  result["name"] = %obj.name
  if obj.parentTeamId.isSome():
    result["parent_team_id"] = %obj.parentTeamId.get()
  result["tenant_id"] = %obj.tenantId
  result["updated_at"] = %obj.updatedAt

