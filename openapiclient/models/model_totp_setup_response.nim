#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type TotpSetupResponse* = object
  ## 
  backupCodes*: seq[string]
  qrCodeUrl*: string
  secret*: string


# Custom JSON deserialization for TotpSetupResponse with custom field names
proc to*(node: JsonNode, T: typedesc[TotpSetupResponse]): TotpSetupResponse =
  result = TotpSetupResponse()
  if node.kind == JObject:
    if node.hasKey("backup_codes"):
      result.backupCodes = to(node["backup_codes"], seq[string])
    if node.hasKey("qr_code_url"):
      result.qrCodeUrl = to(node["qr_code_url"], string)
    if node.hasKey("secret"):
      result.secret = to(node["secret"], string)

# Custom JSON serialization for TotpSetupResponse with custom field names
proc `%`*(obj: TotpSetupResponse): JsonNode =
  result = newJObject()
  result["backup_codes"] = %obj.backupCodes
  result["qr_code_url"] = %obj.qrCodeUrl
  result["secret"] = %obj.secret

