#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_smtp_encryption

type SmtpConfig* = object
  ## 
  encryption*: SmtpEncryption
  fromAddress*: string
  fromName*: Option[string]
  host*: string
  password*: string
  port*: int
  timeoutSeconds*: Option[int64]
  username*: string


# Custom JSON deserialization for SmtpConfig with custom field names
proc to*(node: JsonNode, T: typedesc[SmtpConfig]): SmtpConfig =
  result = SmtpConfig()
  if node.kind == JObject:
    if node.hasKey("encryption"):
      result.encryption = to(node["encryption"], SmtpEncryption)
    if node.hasKey("from_address"):
      result.fromAddress = to(node["from_address"], string)
    if node.hasKey("from_name") and node["from_name"].kind != JNull:
      result.fromName = some(to(node["from_name"], typeof(result.fromName.get())))
    if node.hasKey("host"):
      result.host = to(node["host"], string)
    if node.hasKey("password"):
      result.password = to(node["password"], string)
    if node.hasKey("port"):
      result.port = to(node["port"], int)
    if node.hasKey("timeout_seconds") and node["timeout_seconds"].kind != JNull:
      result.timeoutSeconds = some(to(node["timeout_seconds"], typeof(result.timeoutSeconds.get())))
    if node.hasKey("username"):
      result.username = to(node["username"], string)

# Custom JSON serialization for SmtpConfig with custom field names
proc `%`*(obj: SmtpConfig): JsonNode =
  result = newJObject()
  result["encryption"] = %obj.encryption
  result["from_address"] = %obj.fromAddress
  if obj.fromName.isSome():
    result["from_name"] = %obj.fromName.get()
  result["host"] = %obj.host
  result["password"] = %obj.password
  result["port"] = %obj.port
  if obj.timeoutSeconds.isSome():
    result["timeout_seconds"] = %obj.timeoutSeconds.get()
  result["username"] = %obj.username

