#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_datev_import_row

type DatevImportResponse* = object
  ## 
  count*: int
  filename*: string
  rows*: seq[DatevImportRow]


# Custom JSON deserialization for DatevImportResponse with custom field names
proc to*(node: JsonNode, T: typedesc[DatevImportResponse]): DatevImportResponse =
  result = DatevImportResponse()
  if node.kind == JObject:
    if node.hasKey("count"):
      result.count = to(node["count"], int)
    if node.hasKey("filename"):
      result.filename = to(node["filename"], string)
    if node.hasKey("rows"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["rows"]
      if arrayNode.kind == JArray:
        result.rows = @[]
        for item in arrayNode.items:
          result.rows.add(to(item, DatevImportRow))

# Custom JSON serialization for DatevImportResponse with custom field names
proc `%`*(obj: DatevImportResponse): JsonNode =
  result = newJObject()
  result["count"] = %obj.count
  result["filename"] = %obj.filename
  result["rows"] = %obj.rows

