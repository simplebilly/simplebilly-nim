#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type

type CreateChannelDto* = object
  ## 
  channelType*: string
  config*: Option[JsonNode]
  name*: string


# Custom JSON deserialization for CreateChannelDto with custom field names
proc to*(node: JsonNode, T: typedesc[CreateChannelDto]): CreateChannelDto =
  result = CreateChannelDto()
  if node.kind == JObject:
    if node.hasKey("channel_type"):
      result.channelType = to(node["channel_type"], string)
    if node.hasKey("config") and node["config"].kind != JNull:
      result.config = some(to(node["config"], typeof(result.config.get())))
    if node.hasKey("name"):
      result.name = to(node["name"], string)

# Custom JSON serialization for CreateChannelDto with custom field names
proc `%`*(obj: CreateChannelDto): JsonNode =
  result = newJObject()
  result["channel_type"] = %obj.channelType
  if obj.config.isSome():
    result["config"] = %obj.config.get()
  result["name"] = %obj.name

