#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type PayrollAutopayPayload* = object
  ## 
  debtorBic*: Option[string]
  debtorIban*: Option[string]
  debtorName*: Option[string]
  executionDate*: Option[string]


# Custom JSON deserialization for PayrollAutopayPayload with custom field names
proc to*(node: JsonNode, T: typedesc[PayrollAutopayPayload]): PayrollAutopayPayload =
  result = PayrollAutopayPayload()
  if node.kind == JObject:
    if node.hasKey("debtor_bic") and node["debtor_bic"].kind != JNull:
      result.debtorBic = some(to(node["debtor_bic"], typeof(result.debtorBic.get())))
    if node.hasKey("debtor_iban") and node["debtor_iban"].kind != JNull:
      result.debtorIban = some(to(node["debtor_iban"], typeof(result.debtorIban.get())))
    if node.hasKey("debtor_name") and node["debtor_name"].kind != JNull:
      result.debtorName = some(to(node["debtor_name"], typeof(result.debtorName.get())))
    if node.hasKey("execution_date") and node["execution_date"].kind != JNull:
      result.executionDate = some(to(node["execution_date"], typeof(result.executionDate.get())))

# Custom JSON serialization for PayrollAutopayPayload with custom field names
proc `%`*(obj: PayrollAutopayPayload): JsonNode =
  result = newJObject()
  if obj.debtorBic.isSome():
    result["debtor_bic"] = %obj.debtorBic.get()
  if obj.debtorIban.isSome():
    result["debtor_iban"] = %obj.debtorIban.get()
  if obj.debtorName.isSome():
    result["debtor_name"] = %obj.debtorName.get()
  if obj.executionDate.isSome():
    result["execution_date"] = %obj.executionDate.get()

