#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type UserTenantInfo* = object
  ## 
  customDomain*: Option[string]
  role*: string
  subdomain*: Option[string]
  tenantId*: string
  tenantName*: string


# Custom JSON deserialization for UserTenantInfo with custom field names
proc to*(node: JsonNode, T: typedesc[UserTenantInfo]): UserTenantInfo =
  result = UserTenantInfo()
  if node.kind == JObject:
    if node.hasKey("custom_domain") and node["custom_domain"].kind != JNull:
      result.customDomain = some(to(node["custom_domain"], typeof(result.customDomain.get())))
    if node.hasKey("role"):
      result.role = to(node["role"], string)
    if node.hasKey("subdomain") and node["subdomain"].kind != JNull:
      result.subdomain = some(to(node["subdomain"], typeof(result.subdomain.get())))
    if node.hasKey("tenant_id"):
      result.tenantId = to(node["tenant_id"], string)
    if node.hasKey("tenant_name"):
      result.tenantName = to(node["tenant_name"], string)

# Custom JSON serialization for UserTenantInfo with custom field names
proc `%`*(obj: UserTenantInfo): JsonNode =
  result = newJObject()
  if obj.customDomain.isSome():
    result["custom_domain"] = %obj.customDomain.get()
  result["role"] = %obj.role
  if obj.subdomain.isSome():
    result["subdomain"] = %obj.subdomain.get()
  result["tenant_id"] = %obj.tenantId
  result["tenant_name"] = %obj.tenantName

