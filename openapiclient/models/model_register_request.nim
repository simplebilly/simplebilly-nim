#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type RegisterRequest* = object
  ## 
  companyName*: string
  email*: string
  firstName*: string
  lastName*: string
  password*: string
  privacyAccepted*: bool ## GDPR consent — registration is rejected unless true.


# Custom JSON deserialization for RegisterRequest with custom field names
proc to*(node: JsonNode, T: typedesc[RegisterRequest]): RegisterRequest =
  result = RegisterRequest()
  if node.kind == JObject:
    if node.hasKey("company_name"):
      result.companyName = to(node["company_name"], string)
    if node.hasKey("email"):
      result.email = to(node["email"], string)
    if node.hasKey("first_name"):
      result.firstName = to(node["first_name"], string)
    if node.hasKey("last_name"):
      result.lastName = to(node["last_name"], string)
    if node.hasKey("password"):
      result.password = to(node["password"], string)
    if node.hasKey("privacy_accepted"):
      result.privacyAccepted = to(node["privacy_accepted"], bool)

# Custom JSON serialization for RegisterRequest with custom field names
proc `%`*(obj: RegisterRequest): JsonNode =
  result = newJObject()
  result["company_name"] = %obj.companyName
  result["email"] = %obj.email
  result["first_name"] = %obj.firstName
  result["last_name"] = %obj.lastName
  result["password"] = %obj.password
  result["privacy_accepted"] = %obj.privacyAccepted

