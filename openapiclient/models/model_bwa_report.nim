#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_bwa_expenses
import model_bwa_revenue
import model_bwa_summary

type BWAReport* = object
  ## 
  expenses*: BWAExpenses
  generatedAt*: string
  period*: string
  revenue*: BWARevenue
  summary*: BWASummary


# Custom JSON deserialization for BWAReport with custom field names
proc to*(node: JsonNode, T: typedesc[BWAReport]): BWAReport =
  result = BWAReport()
  if node.kind == JObject:
    if node.hasKey("expenses"):
      result.expenses = to(node["expenses"], BWAExpenses)
    if node.hasKey("generated_at"):
      result.generatedAt = to(node["generated_at"], string)
    if node.hasKey("period"):
      result.period = to(node["period"], string)
    if node.hasKey("revenue"):
      result.revenue = to(node["revenue"], BWARevenue)
    if node.hasKey("summary"):
      result.summary = to(node["summary"], BWASummary)

# Custom JSON serialization for BWAReport with custom field names
proc `%`*(obj: BWAReport): JsonNode =
  result = newJObject()
  result["expenses"] = %obj.expenses
  result["generated_at"] = %obj.generatedAt
  result["period"] = %obj.period
  result["revenue"] = %obj.revenue
  result["summary"] = %obj.summary

