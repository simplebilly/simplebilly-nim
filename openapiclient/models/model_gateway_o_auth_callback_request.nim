#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type GatewayOAuthCallbackRequest* = object
  ## 
  code*: string
  gatewayType*: string
  redirectUri*: string
  state*: string


# Custom JSON deserialization for GatewayOAuthCallbackRequest with custom field names
proc to*(node: JsonNode, T: typedesc[GatewayOAuthCallbackRequest]): GatewayOAuthCallbackRequest =
  result = GatewayOAuthCallbackRequest()
  if node.kind == JObject:
    if node.hasKey("code"):
      result.code = to(node["code"], string)
    if node.hasKey("gateway_type"):
      result.gatewayType = to(node["gateway_type"], string)
    if node.hasKey("redirect_uri"):
      result.redirectUri = to(node["redirect_uri"], string)
    if node.hasKey("state"):
      result.state = to(node["state"], string)

# Custom JSON serialization for GatewayOAuthCallbackRequest with custom field names
proc `%`*(obj: GatewayOAuthCallbackRequest): JsonNode =
  result = newJObject()
  result["code"] = %obj.code
  result["gateway_type"] = %obj.gatewayType
  result["redirect_uri"] = %obj.redirectUri
  result["state"] = %obj.state

