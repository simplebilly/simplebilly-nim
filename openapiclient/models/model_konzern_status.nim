#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_konzern_beteiligung
import model_konzern_thresholds

type KonzernStatus* = object
  ## 
  groessenbefreit*: bool
  kapitalmarktorientiert*: bool
  konzernabschlusspflicht*: bool
  missingGroupFigures*: bool ## Keine group_figures-Zeile für das Jahr vorhanden → keine Größenbefreiung.
  mutterunternehmen*: bool ## Mutterunternehmen: mindestens eine beherrschte Beteiligung (§ 290 Abs. 1 HGB).
  parentName*: Option[string] ## Mutterunternehmen für die Zwischenholding-Befreiung (§ 291 HGB).
  parentSitus*: Option[string]
  participations*: seq[KonzernBeteiligung]
  thresholds*: KonzernThresholds
  year*: int
  zwischenholdingBefreit*: bool
  zwischenholdingHinweis*: Option[string] ## Hinweis zu den § 291-Voraussetzungen (EU/EWR-Sitz, geprüfter Konzernabschluss).


# Custom JSON deserialization for KonzernStatus with custom field names
proc to*(node: JsonNode, T: typedesc[KonzernStatus]): KonzernStatus =
  result = KonzernStatus()
  if node.kind == JObject:
    if node.hasKey("groessenbefreit"):
      result.groessenbefreit = to(node["groessenbefreit"], bool)
    if node.hasKey("kapitalmarktorientiert"):
      result.kapitalmarktorientiert = to(node["kapitalmarktorientiert"], bool)
    if node.hasKey("konzernabschlusspflicht"):
      result.konzernabschlusspflicht = to(node["konzernabschlusspflicht"], bool)
    if node.hasKey("missing_group_figures"):
      result.missingGroupFigures = to(node["missing_group_figures"], bool)
    if node.hasKey("mutterunternehmen"):
      result.mutterunternehmen = to(node["mutterunternehmen"], bool)
    if node.hasKey("parent_name") and node["parent_name"].kind != JNull:
      result.parentName = some(to(node["parent_name"], typeof(result.parentName.get())))
    if node.hasKey("parent_situs") and node["parent_situs"].kind != JNull:
      result.parentSitus = some(to(node["parent_situs"], typeof(result.parentSitus.get())))
    if node.hasKey("participations"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["participations"]
      if arrayNode.kind == JArray:
        result.participations = @[]
        for item in arrayNode.items:
          result.participations.add(to(item, KonzernBeteiligung))
    if node.hasKey("thresholds"):
      result.thresholds = to(node["thresholds"], KonzernThresholds)
    if node.hasKey("year"):
      result.year = to(node["year"], int)
    if node.hasKey("zwischenholding_befreit"):
      result.zwischenholdingBefreit = to(node["zwischenholding_befreit"], bool)
    if node.hasKey("zwischenholding_hinweis") and node["zwischenholding_hinweis"].kind != JNull:
      result.zwischenholdingHinweis = some(to(node["zwischenholding_hinweis"], typeof(result.zwischenholdingHinweis.get())))

# Custom JSON serialization for KonzernStatus with custom field names
proc `%`*(obj: KonzernStatus): JsonNode =
  result = newJObject()
  result["groessenbefreit"] = %obj.groessenbefreit
  result["kapitalmarktorientiert"] = %obj.kapitalmarktorientiert
  result["konzernabschlusspflicht"] = %obj.konzernabschlusspflicht
  result["missing_group_figures"] = %obj.missingGroupFigures
  result["mutterunternehmen"] = %obj.mutterunternehmen
  if obj.parentName.isSome():
    result["parent_name"] = %obj.parentName.get()
  if obj.parentSitus.isSome():
    result["parent_situs"] = %obj.parentSitus.get()
  result["participations"] = %obj.participations
  result["thresholds"] = %obj.thresholds
  result["year"] = %obj.year
  result["zwischenholding_befreit"] = %obj.zwischenholdingBefreit
  if obj.zwischenholdingHinweis.isSome():
    result["zwischenholding_hinweis"] = %obj.zwischenholdingHinweis.get()

