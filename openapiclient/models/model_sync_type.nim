#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type SyncType* {.pure.} = enum
  Orders
  Products
  Inventory
  Invoices
  Shipments
  Contacts
  Prices
  Config
  Branding
  Legal

func `%`*(v: SyncType): JsonNode =
  result = case v:
    of SyncType.Orders: %"orders"
    of SyncType.Products: %"products"
    of SyncType.Inventory: %"inventory"
    of SyncType.Invoices: %"invoices"
    of SyncType.Shipments: %"shipments"
    of SyncType.Contacts: %"contacts"
    of SyncType.Prices: %"prices"
    of SyncType.Config: %"config"
    of SyncType.Branding: %"branding"
    of SyncType.Legal: %"legal"

func `$`*(v: SyncType): string =
  result = case v:
    of SyncType.Orders: $("orders")
    of SyncType.Products: $("products")
    of SyncType.Inventory: $("inventory")
    of SyncType.Invoices: $("invoices")
    of SyncType.Shipments: $("shipments")
    of SyncType.Contacts: $("contacts")
    of SyncType.Prices: $("prices")
    of SyncType.Config: $("config")
    of SyncType.Branding: $("branding")
    of SyncType.Legal: $("legal")

proc to*(node: JsonNode, T: typedesc[SyncType]): SyncType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum SyncType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("orders"):
    return SyncType.Orders
  of $("products"):
    return SyncType.Products
  of $("inventory"):
    return SyncType.Inventory
  of $("invoices"):
    return SyncType.Invoices
  of $("shipments"):
    return SyncType.Shipments
  of $("contacts"):
    return SyncType.Contacts
  of $("prices"):
    return SyncType.Prices
  of $("config"):
    return SyncType.Config
  of $("branding"):
    return SyncType.Branding
  of $("legal"):
    return SyncType.Legal
  else:
    raise newException(ValueError, "Invalid enum value for SyncType: " & strVal)

