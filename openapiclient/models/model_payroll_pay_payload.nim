#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type PayrollPayPayload* = object
  ## 
  paymentDate*: string


# Custom JSON deserialization for PayrollPayPayload with custom field names
proc to*(node: JsonNode, T: typedesc[PayrollPayPayload]): PayrollPayPayload =
  result = PayrollPayPayload()
  if node.kind == JObject:
    if node.hasKey("payment_date"):
      result.paymentDate = to(node["payment_date"], string)

# Custom JSON serialization for PayrollPayPayload with custom field names
proc `%`*(obj: PayrollPayPayload): JsonNode =
  result = newJObject()
  result["payment_date"] = %obj.paymentDate

