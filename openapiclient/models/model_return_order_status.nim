#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type ReturnOrderStatus* {.pure.} = enum
  Requested
  Received
  Inspected
  Restocked
  Closed

func `%`*(v: ReturnOrderStatus): JsonNode =
  result = case v:
    of ReturnOrderStatus.Requested: %"requested"
    of ReturnOrderStatus.Received: %"received"
    of ReturnOrderStatus.Inspected: %"inspected"
    of ReturnOrderStatus.Restocked: %"restocked"
    of ReturnOrderStatus.Closed: %"closed"

func `$`*(v: ReturnOrderStatus): string =
  result = case v:
    of ReturnOrderStatus.Requested: $("requested")
    of ReturnOrderStatus.Received: $("received")
    of ReturnOrderStatus.Inspected: $("inspected")
    of ReturnOrderStatus.Restocked: $("restocked")
    of ReturnOrderStatus.Closed: $("closed")

proc to*(node: JsonNode, T: typedesc[ReturnOrderStatus]): ReturnOrderStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum ReturnOrderStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("requested"):
    return ReturnOrderStatus.Requested
  of $("received"):
    return ReturnOrderStatus.Received
  of $("inspected"):
    return ReturnOrderStatus.Inspected
  of $("restocked"):
    return ReturnOrderStatus.Restocked
  of $("closed"):
    return ReturnOrderStatus.Closed
  else:
    raise newException(ValueError, "Invalid enum value for ReturnOrderStatus: " & strVal)

