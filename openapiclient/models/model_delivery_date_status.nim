#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type DeliveryDateStatus* {.pure.} = enum
  Promised
  Confirmed
  Rescheduled
  Fulfilled
  Late
  Cancelled

func `%`*(v: DeliveryDateStatus): JsonNode =
  result = case v:
    of DeliveryDateStatus.Promised: %"promised"
    of DeliveryDateStatus.Confirmed: %"confirmed"
    of DeliveryDateStatus.Rescheduled: %"rescheduled"
    of DeliveryDateStatus.Fulfilled: %"fulfilled"
    of DeliveryDateStatus.Late: %"late"
    of DeliveryDateStatus.Cancelled: %"cancelled"

func `$`*(v: DeliveryDateStatus): string =
  result = case v:
    of DeliveryDateStatus.Promised: $("promised")
    of DeliveryDateStatus.Confirmed: $("confirmed")
    of DeliveryDateStatus.Rescheduled: $("rescheduled")
    of DeliveryDateStatus.Fulfilled: $("fulfilled")
    of DeliveryDateStatus.Late: $("late")
    of DeliveryDateStatus.Cancelled: $("cancelled")

proc to*(node: JsonNode, T: typedesc[DeliveryDateStatus]): DeliveryDateStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum DeliveryDateStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("promised"):
    return DeliveryDateStatus.Promised
  of $("confirmed"):
    return DeliveryDateStatus.Confirmed
  of $("rescheduled"):
    return DeliveryDateStatus.Rescheduled
  of $("fulfilled"):
    return DeliveryDateStatus.Fulfilled
  of $("late"):
    return DeliveryDateStatus.Late
  of $("cancelled"):
    return DeliveryDateStatus.Cancelled
  else:
    raise newException(ValueError, "Invalid enum value for DeliveryDateStatus: " & strVal)

