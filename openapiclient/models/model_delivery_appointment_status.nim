#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type DeliveryAppointmentStatus* {.pure.} = enum
  Requested
  Confirmed
  Arrived
  Cancelled
  Completed

func `%`*(v: DeliveryAppointmentStatus): JsonNode =
  result = case v:
    of DeliveryAppointmentStatus.Requested: %"requested"
    of DeliveryAppointmentStatus.Confirmed: %"confirmed"
    of DeliveryAppointmentStatus.Arrived: %"arrived"
    of DeliveryAppointmentStatus.Cancelled: %"cancelled"
    of DeliveryAppointmentStatus.Completed: %"completed"

func `$`*(v: DeliveryAppointmentStatus): string =
  result = case v:
    of DeliveryAppointmentStatus.Requested: $("requested")
    of DeliveryAppointmentStatus.Confirmed: $("confirmed")
    of DeliveryAppointmentStatus.Arrived: $("arrived")
    of DeliveryAppointmentStatus.Cancelled: $("cancelled")
    of DeliveryAppointmentStatus.Completed: $("completed")

proc to*(node: JsonNode, T: typedesc[DeliveryAppointmentStatus]): DeliveryAppointmentStatus =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum DeliveryAppointmentStatus, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("requested"):
    return DeliveryAppointmentStatus.Requested
  of $("confirmed"):
    return DeliveryAppointmentStatus.Confirmed
  of $("arrived"):
    return DeliveryAppointmentStatus.Arrived
  of $("cancelled"):
    return DeliveryAppointmentStatus.Cancelled
  of $("completed"):
    return DeliveryAppointmentStatus.Completed
  else:
    raise newException(ValueError, "Invalid enum value for DeliveryAppointmentStatus: " & strVal)

