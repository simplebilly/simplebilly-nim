#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_euer_kat_summe

type EuerZeileDetail* = object
  ## 
  abschnitt*: string
  betragGesamt*: string
  bezeichnung*: string
  kategorien*: seq[EuerKatSumme]
  zeile*: int


# Custom JSON deserialization for EuerZeileDetail with custom field names
proc to*(node: JsonNode, T: typedesc[EuerZeileDetail]): EuerZeileDetail =
  result = EuerZeileDetail()
  if node.kind == JObject:
    if node.hasKey("abschnitt"):
      result.abschnitt = to(node["abschnitt"], string)
    if node.hasKey("betrag_gesamt"):
      result.betragGesamt = to(node["betrag_gesamt"], string)
    if node.hasKey("bezeichnung"):
      result.bezeichnung = to(node["bezeichnung"], string)
    if node.hasKey("kategorien"):
      result.kategorien = to(node["kategorien"], seq[EuerKatSumme])
    if node.hasKey("zeile"):
      result.zeile = to(node["zeile"], int)

# Custom JSON serialization for EuerZeileDetail with custom field names
proc `%`*(obj: EuerZeileDetail): JsonNode =
  result = newJObject()
  result["abschnitt"] = %obj.abschnitt
  result["betrag_gesamt"] = %obj.betragGesamt
  result["bezeichnung"] = %obj.bezeichnung
  result["kategorien"] = %obj.kategorien
  result["zeile"] = %obj.zeile

