#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_frist_eintrag

type FristenErgebnis* = object
  ## 
  anzahl*: int
  fristen*: seq[FristEintrag]


# Custom JSON deserialization for FristenErgebnis with custom field names
proc to*(node: JsonNode, T: typedesc[FristenErgebnis]): FristenErgebnis =
  result = FristenErgebnis()
  if node.kind == JObject:
    if node.hasKey("anzahl"):
      result.anzahl = to(node["anzahl"], int)
    if node.hasKey("fristen"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["fristen"]
      if arrayNode.kind == JArray:
        result.fristen = @[]
        for item in arrayNode.items:
          result.fristen.add(to(item, FristEintrag))

# Custom JSON serialization for FristenErgebnis with custom field names
proc `%`*(obj: FristenErgebnis): JsonNode =
  result = newJObject()
  result["anzahl"] = %obj.anzahl
  result["fristen"] = %obj.fristen

