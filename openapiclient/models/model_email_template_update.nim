#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type
import model_email_template_status

type EmailTemplateUpdate* = object
  ## 
  body*: Option[string] ## E-mail body with optional placeholders.
  name*: Option[string] ## Human-readable template name, e.g. \"Follow-up after quote\".
  status*: Option[EmailTemplateStatus] ## One of: active | inactive
  subject*: Option[string] ## E-mail subject line with optional placeholders.
  variables*: Option[JsonNode] ## Placeholders used by this template, e.g. `[\"contact.first_name\"]`.

