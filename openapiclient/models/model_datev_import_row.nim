#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type DatevImportRow* = object
  ## 
  account*: string
  amount*: string
  baseAmount*: string
  baseCurrency*: string
  bookingText*: string
  buKey*: string
  costCenter1*: string
  costCenter2*: string
  currency*: string
  debitCredit*: string
  discount*: string
  documentDate*: string
  documentField2*: string
  documentNumber*: string
  euCountryVatId*: string
  euTaxRate*: string
  exchangeRate*: string
  oppositeAccount*: string


# Custom JSON deserialization for DatevImportRow with custom field names
proc to*(node: JsonNode, T: typedesc[DatevImportRow]): DatevImportRow =
  result = DatevImportRow()
  if node.kind == JObject:
    if node.hasKey("account"):
      result.account = to(node["account"], string)
    if node.hasKey("amount"):
      result.amount = to(node["amount"], string)
    if node.hasKey("base_amount"):
      result.baseAmount = to(node["base_amount"], string)
    if node.hasKey("base_currency"):
      result.baseCurrency = to(node["base_currency"], string)
    if node.hasKey("booking_text"):
      result.bookingText = to(node["booking_text"], string)
    if node.hasKey("bu_key"):
      result.buKey = to(node["bu_key"], string)
    if node.hasKey("cost_center1"):
      result.costCenter1 = to(node["cost_center1"], string)
    if node.hasKey("cost_center2"):
      result.costCenter2 = to(node["cost_center2"], string)
    if node.hasKey("currency"):
      result.currency = to(node["currency"], string)
    if node.hasKey("debit_credit"):
      result.debitCredit = to(node["debit_credit"], string)
    if node.hasKey("discount"):
      result.discount = to(node["discount"], string)
    if node.hasKey("document_date"):
      result.documentDate = to(node["document_date"], string)
    if node.hasKey("document_field2"):
      result.documentField2 = to(node["document_field2"], string)
    if node.hasKey("document_number"):
      result.documentNumber = to(node["document_number"], string)
    if node.hasKey("eu_country_vat_id"):
      result.euCountryVatId = to(node["eu_country_vat_id"], string)
    if node.hasKey("eu_tax_rate"):
      result.euTaxRate = to(node["eu_tax_rate"], string)
    if node.hasKey("exchange_rate"):
      result.exchangeRate = to(node["exchange_rate"], string)
    if node.hasKey("opposite_account"):
      result.oppositeAccount = to(node["opposite_account"], string)

# Custom JSON serialization for DatevImportRow with custom field names
proc `%`*(obj: DatevImportRow): JsonNode =
  result = newJObject()
  result["account"] = %obj.account
  result["amount"] = %obj.amount
  result["base_amount"] = %obj.baseAmount
  result["base_currency"] = %obj.baseCurrency
  result["booking_text"] = %obj.bookingText
  result["bu_key"] = %obj.buKey
  result["cost_center1"] = %obj.costCenter1
  result["cost_center2"] = %obj.costCenter2
  result["currency"] = %obj.currency
  result["debit_credit"] = %obj.debitCredit
  result["discount"] = %obj.discount
  result["document_date"] = %obj.documentDate
  result["document_field2"] = %obj.documentField2
  result["document_number"] = %obj.documentNumber
  result["eu_country_vat_id"] = %obj.euCountryVatId
  result["eu_tax_rate"] = %obj.euTaxRate
  result["exchange_rate"] = %obj.exchangeRate
  result["opposite_account"] = %obj.oppositeAccount

