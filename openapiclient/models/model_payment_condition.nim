#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type PaymentCondition* = object
  ## 
  discountDays*: int
  discountPercentage*: float64
  id*: string
  name*: string
  paymentTermDays*: int


# Custom JSON deserialization for PaymentCondition with custom field names
proc to*(node: JsonNode, T: typedesc[PaymentCondition]): PaymentCondition =
  result = PaymentCondition()
  if node.kind == JObject:
    if node.hasKey("discount_days"):
      result.discountDays = to(node["discount_days"], int)
    if node.hasKey("discount_percentage"):
      result.discountPercentage = to(node["discount_percentage"], float64)
    if node.hasKey("id"):
      result.id = to(node["id"], string)
    if node.hasKey("name"):
      result.name = to(node["name"], string)
    if node.hasKey("payment_term_days"):
      result.paymentTermDays = to(node["payment_term_days"], int)

# Custom JSON serialization for PaymentCondition with custom field names
proc `%`*(obj: PaymentCondition): JsonNode =
  result = newJObject()
  result["discount_days"] = %obj.discountDays
  result["discount_percentage"] = %obj.discountPercentage
  result["id"] = %obj.id
  result["name"] = %obj.name
  result["payment_term_days"] = %obj.paymentTermDays

