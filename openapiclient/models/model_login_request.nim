#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type LoginRequest* = object
  ## 
  email*: string
  password*: string
  totpCode*: Option[string]


# Custom JSON deserialization for LoginRequest with custom field names
proc to*(node: JsonNode, T: typedesc[LoginRequest]): LoginRequest =
  result = LoginRequest()
  if node.kind == JObject:
    if node.hasKey("email"):
      result.email = to(node["email"], string)
    if node.hasKey("password"):
      result.password = to(node["password"], string)
    if node.hasKey("totp_code") and node["totp_code"].kind != JNull:
      result.totpCode = some(to(node["totp_code"], typeof(result.totpCode.get())))

# Custom JSON serialization for LoginRequest with custom field names
proc `%`*(obj: LoginRequest): JsonNode =
  result = newJObject()
  result["email"] = %obj.email
  result["password"] = %obj.password
  if obj.totpCode.isSome():
    result["totp_code"] = %obj.totpCode.get()

