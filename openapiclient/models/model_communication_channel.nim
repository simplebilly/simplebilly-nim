#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type CommunicationChannel* {.pure.} = enum
  Email
  Call
  Meeting
  Chat
  Note

func `%`*(v: CommunicationChannel): JsonNode =
  result = case v:
    of CommunicationChannel.Email: %"email"
    of CommunicationChannel.Call: %"call"
    of CommunicationChannel.Meeting: %"meeting"
    of CommunicationChannel.Chat: %"chat"
    of CommunicationChannel.Note: %"note"

func `$`*(v: CommunicationChannel): string =
  result = case v:
    of CommunicationChannel.Email: $("email")
    of CommunicationChannel.Call: $("call")
    of CommunicationChannel.Meeting: $("meeting")
    of CommunicationChannel.Chat: $("chat")
    of CommunicationChannel.Note: $("note")

proc to*(node: JsonNode, T: typedesc[CommunicationChannel]): CommunicationChannel =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum CommunicationChannel, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("email"):
    return CommunicationChannel.Email
  of $("call"):
    return CommunicationChannel.Call
  of $("meeting"):
    return CommunicationChannel.Meeting
  of $("chat"):
    return CommunicationChannel.Chat
  of $("note"):
    return CommunicationChannel.Note
  else:
    raise newException(ValueError, "Invalid enum value for CommunicationChannel: " & strVal)

