#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type JobTitleGap* = object
  ## 
  employeeCount*: int
  femaleMeanHourly*: string
  jobTitle*: string
  maleMeanHourly*: string
  meanGapPct*: float64
  medianGapPct*: float64


# Custom JSON deserialization for JobTitleGap with custom field names
proc to*(node: JsonNode, T: typedesc[JobTitleGap]): JobTitleGap =
  result = JobTitleGap()
  if node.kind == JObject:
    if node.hasKey("employee_count"):
      result.employeeCount = to(node["employee_count"], int)
    if node.hasKey("female_mean_hourly"):
      result.femaleMeanHourly = to(node["female_mean_hourly"], string)
    if node.hasKey("job_title"):
      result.jobTitle = to(node["job_title"], string)
    if node.hasKey("male_mean_hourly"):
      result.maleMeanHourly = to(node["male_mean_hourly"], string)
    if node.hasKey("mean_gap_pct"):
      result.meanGapPct = to(node["mean_gap_pct"], float64)
    if node.hasKey("median_gap_pct"):
      result.medianGapPct = to(node["median_gap_pct"], float64)

# Custom JSON serialization for JobTitleGap with custom field names
proc `%`*(obj: JobTitleGap): JsonNode =
  result = newJObject()
  result["employee_count"] = %obj.employeeCount
  result["female_mean_hourly"] = %obj.femaleMeanHourly
  result["job_title"] = %obj.jobTitle
  result["male_mean_hourly"] = %obj.maleMeanHourly
  result["mean_gap_pct"] = %obj.meanGapPct
  result["median_gap_pct"] = %obj.medianGapPct

