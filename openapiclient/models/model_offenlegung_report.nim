#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_offenlegung_item

type OffenlegungReport* = object
  ## 
  deadline*: string ## Fristende (Abschlussstichtag + Frist).
  deadlineMonths*: int ## Offenlegungsfrist in Monaten (§ 325 Abs. 4 HGB).
  items*: seq[OffenlegungItem]
  kapitalmarktorientiert*: bool ## Annahme über die Kapitalmarktorientierung.
  note*: string
  year*: int ## Berichtsjahr (laufendes Kalenderjahr).


# Custom JSON deserialization for OffenlegungReport with custom field names
proc to*(node: JsonNode, T: typedesc[OffenlegungReport]): OffenlegungReport =
  result = OffenlegungReport()
  if node.kind == JObject:
    if node.hasKey("deadline"):
      result.deadline = to(node["deadline"], string)
    if node.hasKey("deadline_months"):
      result.deadlineMonths = to(node["deadline_months"], int)
    if node.hasKey("items"):
      result.items = to(node["items"], seq[OffenlegungItem])
    if node.hasKey("kapitalmarktorientiert"):
      result.kapitalmarktorientiert = to(node["kapitalmarktorientiert"], bool)
    if node.hasKey("note"):
      result.note = to(node["note"], string)
    if node.hasKey("year"):
      result.year = to(node["year"], int)

# Custom JSON serialization for OffenlegungReport with custom field names
proc `%`*(obj: OffenlegungReport): JsonNode =
  result = newJObject()
  result["deadline"] = %obj.deadline
  result["deadline_months"] = %obj.deadlineMonths
  result["items"] = %obj.items
  result["kapitalmarktorientiert"] = %obj.kapitalmarktorientiert
  result["note"] = %obj.note
  result["year"] = %obj.year

