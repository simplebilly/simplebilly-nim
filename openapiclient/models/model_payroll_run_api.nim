#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_payroll_entry_api
import model_payroll_run_status

type PayrollRunApi* = object
  ## 
  approvedAt*: Option[string]
  approvedBy*: Option[string]
  createdAt*: string
  entries*: seq[PayrollEntryApi]
  month*: int
  paymentDate*: Option[string]
  periodLabel*: string
  runId*: string
  status*: PayrollRunStatus
  tenantId*: string
  totalEmployeeCount*: int
  totalEmployerCost*: string
  totalGross*: string
  totalNet*: string
  totalSocialSecurity*: string
  totalTaxes*: string
  updatedAt*: Option[string]
  year*: int


# Custom JSON deserialization for PayrollRunApi with custom field names
proc to*(node: JsonNode, T: typedesc[PayrollRunApi]): PayrollRunApi =
  result = PayrollRunApi()
  if node.kind == JObject:
    if node.hasKey("approved_at") and node["approved_at"].kind != JNull:
      result.approvedAt = some(to(node["approved_at"], typeof(result.approvedAt.get())))
    if node.hasKey("approved_by") and node["approved_by"].kind != JNull:
      result.approvedBy = some(to(node["approved_by"], typeof(result.approvedBy.get())))
    if node.hasKey("created_at"):
      result.createdAt = to(node["created_at"], string)
    if node.hasKey("entries"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["entries"]
      if arrayNode.kind == JArray:
        result.entries = @[]
        for item in arrayNode.items:
          result.entries.add(to(item, PayrollEntryApi))
    if node.hasKey("month"):
      result.month = to(node["month"], int)
    if node.hasKey("payment_date") and node["payment_date"].kind != JNull:
      result.paymentDate = some(to(node["payment_date"], typeof(result.paymentDate.get())))
    if node.hasKey("period_label"):
      result.periodLabel = to(node["period_label"], string)
    if node.hasKey("run_id"):
      result.runId = to(node["run_id"], string)
    if node.hasKey("status"):
      result.status = to(node["status"], PayrollRunStatus)
    if node.hasKey("tenant_id"):
      result.tenantId = to(node["tenant_id"], string)
    if node.hasKey("total_employee_count"):
      result.totalEmployeeCount = to(node["total_employee_count"], int)
    if node.hasKey("total_employer_cost"):
      result.totalEmployerCost = to(node["total_employer_cost"], string)
    if node.hasKey("total_gross"):
      result.totalGross = to(node["total_gross"], string)
    if node.hasKey("total_net"):
      result.totalNet = to(node["total_net"], string)
    if node.hasKey("total_social_security"):
      result.totalSocialSecurity = to(node["total_social_security"], string)
    if node.hasKey("total_taxes"):
      result.totalTaxes = to(node["total_taxes"], string)
    if node.hasKey("updated_at") and node["updated_at"].kind != JNull:
      result.updatedAt = some(to(node["updated_at"], typeof(result.updatedAt.get())))
    if node.hasKey("year"):
      result.year = to(node["year"], int)

# Custom JSON serialization for PayrollRunApi with custom field names
proc `%`*(obj: PayrollRunApi): JsonNode =
  result = newJObject()
  if obj.approvedAt.isSome():
    result["approved_at"] = %obj.approvedAt.get()
  if obj.approvedBy.isSome():
    result["approved_by"] = %obj.approvedBy.get()
  result["created_at"] = %obj.createdAt
  result["entries"] = %obj.entries
  result["month"] = %obj.month
  if obj.paymentDate.isSome():
    result["payment_date"] = %obj.paymentDate.get()
  result["period_label"] = %obj.periodLabel
  result["run_id"] = %obj.runId
  result["status"] = %obj.status
  result["tenant_id"] = %obj.tenantId
  result["total_employee_count"] = %obj.totalEmployeeCount
  result["total_employer_cost"] = %obj.totalEmployerCost
  result["total_gross"] = %obj.totalGross
  result["total_net"] = %obj.totalNet
  result["total_social_security"] = %obj.totalSocialSecurity
  result["total_taxes"] = %obj.totalTaxes
  if obj.updatedAt.isSome():
    result["updated_at"] = %obj.updatedAt.get()
  result["year"] = %obj.year

