#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type CategoryTotal* = object
  ## 
  categoryId*: string
  sharePct*: float64
  tco2e*: string


# Custom JSON deserialization for CategoryTotal with custom field names
proc to*(node: JsonNode, T: typedesc[CategoryTotal]): CategoryTotal =
  result = CategoryTotal()
  if node.kind == JObject:
    if node.hasKey("category_id"):
      result.categoryId = to(node["category_id"], string)
    if node.hasKey("share_pct"):
      result.sharePct = to(node["share_pct"], float64)
    if node.hasKey("tco2e"):
      result.tco2e = to(node["tco2e"], string)

# Custom JSON serialization for CategoryTotal with custom field names
proc `%`*(obj: CategoryTotal): JsonNode =
  result = newJObject()
  result["category_id"] = %obj.categoryId
  result["share_pct"] = %obj.sharePct
  result["tco2e"] = %obj.tco2e

