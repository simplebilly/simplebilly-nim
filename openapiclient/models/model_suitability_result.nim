#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_box_fit
import model_method_suitability

type SuitabilityResult* = object
  ## 
  methods*: seq[MethodSuitability]
  recommendedBox*: Option[BoxFit]
  requiresInsurance*: bool
  totalValue*: string
  totalWeightKg*: float64


# Custom JSON deserialization for SuitabilityResult with custom field names
proc to*(node: JsonNode, T: typedesc[SuitabilityResult]): SuitabilityResult =
  result = SuitabilityResult()
  if node.kind == JObject:
    if node.hasKey("methods"):
      # Array of types with custom JSON - manually iterate and deserialize
      let arrayNode = node["methods"]
      if arrayNode.kind == JArray:
        result.methods = @[]
        for item in arrayNode.items:
          result.methods.add(to(item, MethodSuitability))
    if node.hasKey("recommended_box") and node["recommended_box"].kind != JNull:
      result.recommendedBox = some(to(node["recommended_box"], typeof(result.recommendedBox.get())))
    if node.hasKey("requires_insurance"):
      result.requiresInsurance = to(node["requires_insurance"], bool)
    if node.hasKey("total_value"):
      result.totalValue = to(node["total_value"], string)
    if node.hasKey("total_weight_kg"):
      result.totalWeightKg = to(node["total_weight_kg"], float64)

# Custom JSON serialization for SuitabilityResult with custom field names
proc `%`*(obj: SuitabilityResult): JsonNode =
  result = newJObject()
  result["methods"] = %obj.methods
  if obj.recommendedBox.isSome():
    result["recommended_box"] = %obj.recommendedBox.get()
  result["requires_insurance"] = %obj.requiresInsurance
  result["total_value"] = %obj.totalValue
  result["total_weight_kg"] = %obj.totalWeightKg

