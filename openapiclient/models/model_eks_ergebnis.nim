#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_eks_monats_wert

type EksErgebnis* = object
  ## 
  gesamtergebnis*: string
  monate*: seq[EksMonatsWert]
  prognoseNaechste6Monate*: string
  summeAusgaben*: string
  summeEinnahmen*: string
  zeitraumBis*: string
  zeitraumVon*: string


# Custom JSON deserialization for EksErgebnis with custom field names
proc to*(node: JsonNode, T: typedesc[EksErgebnis]): EksErgebnis =
  result = EksErgebnis()
  if node.kind == JObject:
    if node.hasKey("gesamtergebnis"):
      result.gesamtergebnis = to(node["gesamtergebnis"], string)
    if node.hasKey("monate"):
      result.monate = to(node["monate"], seq[EksMonatsWert])
    if node.hasKey("prognose_naechste_6_monate"):
      result.prognoseNaechste6Monate = to(node["prognose_naechste_6_monate"], string)
    if node.hasKey("summe_ausgaben"):
      result.summeAusgaben = to(node["summe_ausgaben"], string)
    if node.hasKey("summe_einnahmen"):
      result.summeEinnahmen = to(node["summe_einnahmen"], string)
    if node.hasKey("zeitraum_bis"):
      result.zeitraumBis = to(node["zeitraum_bis"], string)
    if node.hasKey("zeitraum_von"):
      result.zeitraumVon = to(node["zeitraum_von"], string)

# Custom JSON serialization for EksErgebnis with custom field names
proc `%`*(obj: EksErgebnis): JsonNode =
  result = newJObject()
  result["gesamtergebnis"] = %obj.gesamtergebnis
  result["monate"] = %obj.monate
  result["prognose_naechste_6_monate"] = %obj.prognoseNaechste6Monate
  result["summe_ausgaben"] = %obj.summeAusgaben
  result["summe_einnahmen"] = %obj.summeEinnahmen
  result["zeitraum_bis"] = %obj.zeitraumBis
  result["zeitraum_von"] = %obj.zeitraumVon

