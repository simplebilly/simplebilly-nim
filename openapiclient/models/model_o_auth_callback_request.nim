#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_any_type

type OAuthCallbackRequest* = object
  ## 
  code*: string
  config*: Option[JsonNode]
  connectionId*: Option[string]
  platform*: string
  shopDomain*: Option[string]
  state*: string


# Custom JSON deserialization for OAuthCallbackRequest with custom field names
proc to*(node: JsonNode, T: typedesc[OAuthCallbackRequest]): OAuthCallbackRequest =
  result = OAuthCallbackRequest()
  if node.kind == JObject:
    if node.hasKey("code"):
      result.code = to(node["code"], string)
    if node.hasKey("config") and node["config"].kind != JNull:
      result.config = some(to(node["config"], typeof(result.config.get())))
    if node.hasKey("connection_id") and node["connection_id"].kind != JNull:
      result.connectionId = some(to(node["connection_id"], typeof(result.connectionId.get())))
    if node.hasKey("platform"):
      result.platform = to(node["platform"], string)
    if node.hasKey("shop_domain") and node["shop_domain"].kind != JNull:
      result.shopDomain = some(to(node["shop_domain"], typeof(result.shopDomain.get())))
    if node.hasKey("state"):
      result.state = to(node["state"], string)

# Custom JSON serialization for OAuthCallbackRequest with custom field names
proc `%`*(obj: OAuthCallbackRequest): JsonNode =
  result = newJObject()
  result["code"] = %obj.code
  if obj.config.isSome():
    result["config"] = %obj.config.get()
  if obj.connectionId.isSome():
    result["connection_id"] = %obj.connectionId.get()
  result["platform"] = %obj.platform
  if obj.shopDomain.isSome():
    result["shop_domain"] = %obj.shopDomain.get()
  result["state"] = %obj.state

