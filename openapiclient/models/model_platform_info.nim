#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options

import model_changelog_entry
import model_config_field_info
import model_plugin_pricing

type PlatformInfo* = object
  ## Public metadata for one registered plugin (admin UI). Maps 1:1 from [`plugin_core::PluginInfo`] (same field shape as before).
  author*: string
  changelog*: seq[ChangelogEntry]
  configFieldNames*: seq[string]
  configFields*: seq[ConfigFieldInfo]
  displayName*: string
  platform*: string
  pricing*: PluginPricing
  supportedEntities*: seq[string]
  supportsExport*: bool
  supportsImport*: bool
  supportsOauth*: bool
  version*: string


# Custom JSON deserialization for PlatformInfo with custom field names
proc to*(node: JsonNode, T: typedesc[PlatformInfo]): PlatformInfo =
  result = PlatformInfo()
  if node.kind == JObject:
    if node.hasKey("author"):
      result.author = to(node["author"], string)
    if node.hasKey("changelog"):
      result.changelog = to(node["changelog"], seq[ChangelogEntry])
    if node.hasKey("config_field_names"):
      result.configFieldNames = to(node["config_field_names"], seq[string])
    if node.hasKey("config_fields"):
      result.configFields = to(node["config_fields"], seq[ConfigFieldInfo])
    if node.hasKey("display_name"):
      result.displayName = to(node["display_name"], string)
    if node.hasKey("platform"):
      result.platform = to(node["platform"], string)
    if node.hasKey("pricing"):
      result.pricing = to(node["pricing"], PluginPricing)
    if node.hasKey("supported_entities"):
      result.supportedEntities = to(node["supported_entities"], seq[string])
    if node.hasKey("supports_export"):
      result.supportsExport = to(node["supports_export"], bool)
    if node.hasKey("supports_import"):
      result.supportsImport = to(node["supports_import"], bool)
    if node.hasKey("supports_oauth"):
      result.supportsOauth = to(node["supports_oauth"], bool)
    if node.hasKey("version"):
      result.version = to(node["version"], string)

# Custom JSON serialization for PlatformInfo with custom field names
proc `%`*(obj: PlatformInfo): JsonNode =
  result = newJObject()
  result["author"] = %obj.author
  result["changelog"] = %obj.changelog
  result["config_field_names"] = %obj.configFieldNames
  result["config_fields"] = %obj.configFields
  result["display_name"] = %obj.displayName
  result["platform"] = %obj.platform
  result["pricing"] = %obj.pricing
  result["supported_entities"] = %obj.supportedEntities
  result["supports_export"] = %obj.supportsExport
  result["supports_import"] = %obj.supportsImport
  result["supports_oauth"] = %obj.supportsOauth
  result["version"] = %obj.version

