#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type CompanyType* {.pure.} = enum
  Gmbh
  Ug
  Einzelhaendler
  Freiberufler
  Ag
  Gbr

func `%`*(v: CompanyType): JsonNode =
  result = case v:
    of CompanyType.Gmbh: %"gmbh"
    of CompanyType.Ug: %"ug"
    of CompanyType.Einzelhaendler: %"einzelhaendler"
    of CompanyType.Freiberufler: %"freiberufler"
    of CompanyType.Ag: %"ag"
    of CompanyType.Gbr: %"gbr"

func `$`*(v: CompanyType): string =
  result = case v:
    of CompanyType.Gmbh: $("gmbh")
    of CompanyType.Ug: $("ug")
    of CompanyType.Einzelhaendler: $("einzelhaendler")
    of CompanyType.Freiberufler: $("freiberufler")
    of CompanyType.Ag: $("ag")
    of CompanyType.Gbr: $("gbr")

proc to*(node: JsonNode, T: typedesc[CompanyType]): CompanyType =
  if node.kind != JString:
    raise newException(ValueError, "Expected string for enum CompanyType, got " & $node.kind)
  let strVal = node.getStr()
  case strVal:
  of $("gmbh"):
    return CompanyType.Gmbh
  of $("ug"):
    return CompanyType.Ug
  of $("einzelhaendler"):
    return CompanyType.Einzelhaendler
  of $("freiberufler"):
    return CompanyType.Freiberufler
  of $("ag"):
    return CompanyType.Ag
  of $("gbr"):
    return CompanyType.Gbr
  else:
    raise newException(ValueError, "Invalid enum value for CompanyType: " & strVal)

