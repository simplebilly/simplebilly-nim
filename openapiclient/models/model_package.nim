#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import json
import tables
import marshal
import options


type Package* = object
  ## 
  description*: Option[string]
  heightCm*: Option[float64]
  lengthCm*: Option[float64]
  reference*: Option[string]
  weightKg*: float64
  widthCm*: Option[float64]


# Custom JSON deserialization for Package with custom field names
proc to*(node: JsonNode, T: typedesc[Package]): Package =
  result = Package()
  if node.kind == JObject:
    if node.hasKey("description") and node["description"].kind != JNull:
      result.description = some(to(node["description"], typeof(result.description.get())))
    if node.hasKey("height_cm") and node["height_cm"].kind != JNull:
      result.heightCm = some(to(node["height_cm"], typeof(result.heightCm.get())))
    if node.hasKey("length_cm") and node["length_cm"].kind != JNull:
      result.lengthCm = some(to(node["length_cm"], typeof(result.lengthCm.get())))
    if node.hasKey("reference") and node["reference"].kind != JNull:
      result.reference = some(to(node["reference"], typeof(result.reference.get())))
    if node.hasKey("weight_kg"):
      result.weightKg = to(node["weight_kg"], float64)
    if node.hasKey("width_cm") and node["width_cm"].kind != JNull:
      result.widthCm = some(to(node["width_cm"], typeof(result.widthCm.get())))

# Custom JSON serialization for Package with custom field names
proc `%`*(obj: Package): JsonNode =
  result = newJObject()
  if obj.description.isSome():
    result["description"] = %obj.description.get()
  if obj.heightCm.isSome():
    result["height_cm"] = %obj.heightCm.get()
  if obj.lengthCm.isSome():
    result["length_cm"] = %obj.lengthCm.get()
  if obj.reference.isSome():
    result["reference"] = %obj.reference.get()
  result["weight_kg"] = %obj.weightKg
  if obj.widthCm.isSome():
    result["width_cm"] = %obj.widthCm.get()

