#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_plugin_error
import ../models/model_product
import ../models/model_product_create
import ../models/model_product_stock
import ../models/model_product_update
import ../models/model_stock_update_request

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc createProductApi*(httpClient: HttpClient, productCreate: ProductCreate): (Option[Product], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/products", $(%productCreate))
  constructResult[Product](response)


proc deleteProductApi*(httpClient: HttpClient, productId: stringstring): Response =
  ## 
  httpClient.delete(basepath & fmt"/api/v1/products/{product_id}")



proc getProductApi*(httpClient: HttpClient, productId: stringstring): (Option[Product], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/products/{product_id}")
  constructResult[Product](response)


proc getProductStockApi*(httpClient: HttpClient, productId: stringstring): (Option[ProductStock], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/products/{product_id}/stock")
  constructResult[ProductStock](response)


proc getProductsApi*(httpClient: HttpClient, page: int, pageSize: int, search: string, includeDeleted: bool): (Option[seq[Product]], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  if $page != "":
    query_params_list.add(("page", $page))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $search != "":
    query_params_list.add(("search", $search))
  if $includeDeleted != "":
    query_params_list.add(("include_deleted", $includeDeleted))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/products/" & "?" & url_encoded_query_params)
  constructResult[seq[Product]](response)


proc listLowStockProductsApi*(httpClient: HttpClient, threshold: int64): (Option[seq[ProductStock]], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  if $threshold != "":
    query_params_list.add(("threshold", $threshold))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/products/low-stock" & "?" & url_encoded_query_params)
  constructResult[seq[ProductStock]](response)


proc productRestore*(httpClient: HttpClient, productId: stringstring): (Option[Product], Response) =
  ## 

  let response = httpClient.post(basepath & fmt"/api/v1/products/{product_id}/restore")
  constructResult[Product](response)


proc updateProductApi*(httpClient: HttpClient, productId: stringstring, productUpdate: ProductUpdate): (Option[Product], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.put(basepath & fmt"/api/v1/products/{product_id}", $(%productUpdate))
  constructResult[Product](response)


proc updateProductStockApi*(httpClient: HttpClient, productId: stringstring, stockUpdateRequest: StockUpdateRequest): (Option[ProductStock], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.put(basepath & fmt"/api/v1/products/{product_id}/stock", $(%stockUpdateRequest))
  constructResult[ProductStock](response)

