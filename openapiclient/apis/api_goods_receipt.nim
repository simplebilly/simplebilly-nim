#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_goods_receipt
import ../models/model_plugin_error

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc createGoodsReceipt*(httpClient: HttpClient, goodsReceipt: GoodsReceipt): (Option[GoodsReceipt], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/goods-receipts", $(%goodsReceipt))
  constructResult[GoodsReceipt](response)


proc deleteGoodsReceipt*(httpClient: HttpClient, goodsReceiptId: string): Response =
  ## 
  httpClient.delete(basepath & fmt"/api/v1/goods-receipts/{goods_receipt_id}")



proc getGoodsReceipt*(httpClient: HttpClient, goodsReceiptId: string): (Option[GoodsReceipt], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/goods-receipts/{goods_receipt_id}")
  constructResult[GoodsReceipt](response)


proc listGoodsReceipts*(httpClient: HttpClient, page: int, pageSize: int, purchaseOrderId: string, supplierName: string, warehouseId: string): (Option[seq[GoodsReceipt]], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  if $page != "":
    query_params_list.add(("page", $page))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $purchaseOrderId != "":
    query_params_list.add(("purchase_order_id", $purchaseOrderId))
  if $supplierName != "":
    query_params_list.add(("supplier_name", $supplierName))
  if $warehouseId != "":
    query_params_list.add(("warehouse_id", $warehouseId))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/goods-receipts/" & "?" & url_encoded_query_params)
  constructResult[seq[GoodsReceipt]](response)

