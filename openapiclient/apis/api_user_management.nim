#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_tenant_user
import ../models/model_update_permissions_payload
import ../models/model_update_role_payload

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc getUser*(httpClient: HttpClient, userId: stringstring): (Option[TenantUser], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/users/{user_id}")
  constructResult[TenantUser](response)


proc listUsers*(httpClient: HttpClient): (Option[seq[TenantUser]], Response) =
  ## 

  let response = httpClient.get(basepath & "/api/v1/users")
  constructResult[seq[TenantUser]](response)


proc removeUser*(httpClient: HttpClient, userId: stringstring): Response =
  ## 
  httpClient.delete(basepath & fmt"/api/v1/users/{user_id}")



proc updateUserPermissions*(httpClient: HttpClient, userId: stringstring, updatePermissionsPayload: UpdatePermissionsPayload): Response =
  ## 
  httpClient.headers["Content-Type"] = "application/json"
  httpClient.put(basepath & fmt"/api/v1/users/{user_id}/permissions", $(%updatePermissionsPayload))



proc updateUserRole*(httpClient: HttpClient, userId: stringstring, updateRolePayload: UpdateRolePayload): Response =
  ## 
  httpClient.headers["Content-Type"] = "application/json"
  httpClient.put(basepath & fmt"/api/v1/users/{user_id}/role", $(%updateRolePayload))


