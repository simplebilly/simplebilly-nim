#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_api_response_string
import ../models/model_api_response_team
import ../models/model_api_response_user_profile
import ../models/model_api_response_vec_team
import ../models/model_api_response_vec_user_tenant_info
import ../models/model_change_password_request
import ../models/model_invite_request
import ../models/model_remove_user_request
import ../models/model_team_create
import ../models/model_update_profile_request

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc changePassword*(httpClient: HttpClient, changePasswordRequest: ChangePasswordRequest): Response =
  ## Change the current user's password (requires the current password).
  httpClient.headers["Content-Type"] = "application/json"
  httpClient.post(basepath & "/user/change-password", $(%changePasswordRequest))



proc createTeam*(httpClient: HttpClient, teamCreate: TeamCreate): (Option[ApiResponse_Team], Response) =
  ## Create a new team within the current tenant
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/user/teams", $(%teamCreate))
  constructResult[ApiResponse_Team](response)


proc generateApiKey*(httpClient: HttpClient): (Option[ApiResponse_String], Response) =
  ## Generate a new API key for the current user

  let response = httpClient.post(basepath & "/user/api-key")
  constructResult[ApiResponse_String](response)


proc inviteUser*(httpClient: HttpClient, inviteRequest: InviteRequest): Response =
  ## Invite a user to the current tenant/organization
  httpClient.headers["Content-Type"] = "application/json"
  httpClient.post(basepath & "/user/invite", $(%inviteRequest))



proc listTeams*(httpClient: HttpClient): (Option[ApiResponse_Vec_Team], Response) =
  ## List all teams in the current tenant

  let response = httpClient.get(basepath & "/user/teams")
  constructResult[ApiResponse_Vec_Team](response)


proc removeUserFromOrg*(httpClient: HttpClient, removeUserRequest: RemoveUserRequest): Response =
  ## Remove a user from the current organization
  httpClient.headers["Content-Type"] = "application/json"
  httpClient.request(basepath & "/user/remove", httpMethod = HttpDelete, body = $(%removeUserRequest))



proc updateProfile*(httpClient: HttpClient, updateProfileRequest: UpdateProfileRequest): Response =
  ## Update the current user's profile
  httpClient.headers["Content-Type"] = "application/json"
  httpClient.put(basepath & "/user/profile", $(%updateProfileRequest))



proc userProfile*(httpClient: HttpClient): (Option[ApiResponse_UserProfile], Response) =
  ## Get the current user's profile

  let response = httpClient.get(basepath & "/user/profile")
  constructResult[ApiResponse_UserProfile](response)


proc userTenants*(httpClient: HttpClient): (Option[ApiResponse_Vec_UserTenantInfo], Response) =
  ## List all tenants (organizations) the current user belongs to

  let response = httpClient.get(basepath & "/user/tenants")
  constructResult[ApiResponse_Vec_UserTenantInfo](response)

