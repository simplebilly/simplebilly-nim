#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_application_status_dto
import ../models/model_job_application
import ../models/model_public_posting

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc applyPublic*(httpClient: HttpClient, postingId: stringstring): Response =
  ## 
  httpClient.post(basepath & fmt"/api/v1/public/jobs/{posting_id}/apply")



proc deleteJobApplication*(httpClient: HttpClient, applicationId: stringstring): (Option[JobApplication], Response) =
  ## 

  let response = httpClient.delete(basepath & fmt"/api/v1/job-applications/{application_id}")
  constructResult[JobApplication](response)


proc downloadCv*(httpClient: HttpClient, applicationId: stringstring): Response =
  ## 
  httpClient.get(basepath & fmt"/api/v1/job-applications/{application_id}/cv")



proc getJobApplication*(httpClient: HttpClient, applicationId: stringstring): (Option[JobApplication], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/job-applications/{application_id}")
  constructResult[JobApplication](response)


proc inboundEmail*(httpClient: HttpClient): Response =
  ## Inbound CV email, mailgun/sendgrid inbound-parse style: multipart form with `from`, `subject`, `body-plain` and one or more `attachment-N` file fields. The subject may reference a posting as `[JOB-<posting_id>]`; without one the application lands in the general inbox.
  httpClient.post(basepath & "/api/v1/public/jobs/inbound-email")



proc listJobApplications*(httpClient: HttpClient, postingId: stringstring, status: string, page: int, pageSize: int): (Option[seq[JobApplication]], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  if $postingId != "":
    query_params_list.add(("postingId", $postingId))
  if $status != "":
    query_params_list.add(("status", $status))
  if $page != "":
    query_params_list.add(("page", $page))
  if $pageSize != "":
    query_params_list.add(("pageSize", $pageSize))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/job-applications" & "?" & url_encoded_query_params)
  constructResult[seq[JobApplication]](response)


proc listPublicPostings*(httpClient: HttpClient): (Option[seq[PublicPosting]], Response) =
  ## 

  let response = httpClient.get(basepath & "/api/v1/public/jobs")
  constructResult[seq[PublicPosting]](response)


proc scoreJobApplication*(httpClient: HttpClient, applicationId: stringstring): (Option[JobApplication], Response) =
  ## 

  let response = httpClient.post(basepath & fmt"/api/v1/job-applications/{application_id}/score")
  constructResult[JobApplication](response)


proc updateJobApplicationStatus*(httpClient: HttpClient, applicationId: stringstring, applicationStatusDto: ApplicationStatusDto): (Option[JobApplication], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.patch(basepath & fmt"/api/v1/job-applications/{application_id}/status", $(%applicationStatusDto))
  constructResult[JobApplication](response)

