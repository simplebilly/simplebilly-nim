#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_activity
import ../models/model_activity_status_update
import ../models/model_any_type
import ../models/model_plugin_error

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc createActivity*(httpClient: HttpClient, activity: Activity): (Option[Activity], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/activities", $(%activity))
  constructResult[Activity](response)


proc deleteActivity*(httpClient: HttpClient, activityId: string): Response =
  ## 
  httpClient.delete(basepath & fmt"/api/v1/activities/{activity_id}")



proc getActivity*(httpClient: HttpClient, activityId: string): (Option[Activity], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/activities/{activity_id}")
  constructResult[Activity](response)


proc listActivities*(httpClient: HttpClient, page: int, pageSize: int, contactId: string, activityType: string, status: string, assignedTo: string, overdueOnly: bool): (Option[seq[Activity]], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  if $page != "":
    query_params_list.add(("page", $page))
  if $pageSize != "":
    query_params_list.add(("pageSize", $pageSize))
  if $contactId != "":
    query_params_list.add(("contactId", $contactId))
  if $activityType != "":
    query_params_list.add(("activityType", $activityType))
  if $status != "":
    query_params_list.add(("status", $status))
  if $assignedTo != "":
    query_params_list.add(("assignedTo", $assignedTo))
  if $overdueOnly != "":
    query_params_list.add(("overdueOnly", $overdueOnly))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/activities/" & "?" & url_encoded_query_params)
  constructResult[seq[Activity]](response)


proc updateActivity*(httpClient: HttpClient, activityId: string, body: JsonNode): (Option[Activity], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.put(basepath & fmt"/api/v1/activities/{activity_id}", $(%body))
  constructResult[Activity](response)


proc updateActivityStatus*(httpClient: HttpClient, activityId: string, activityStatusUpdate: ActivityStatusUpdate): (Option[Activity], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.put(basepath & fmt"/api/v1/activities/{activity_id}/status", $(%activityStatusUpdate))
  constructResult[Activity](response)

