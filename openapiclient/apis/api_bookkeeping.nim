#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_allocate_payment_request
import ../models/model_bwa_report
import ../models/model_cashflow_report
import ../models/model_dunning_result
import ../models/model_elster_status
import ../models/model_invoice
import ../models/model_liquidity_position
import ../models/model_plugin_error
import ../models/model_verfahrensdokumentation

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc allocatePaymentApi*(httpClient: HttpClient, allocatePaymentRequest: AllocatePaymentRequest): Response =
  ## Allocate a payment to an invoice
  httpClient.headers["Content-Type"] = "application/json"
  httpClient.post(basepath & "/api/v1/payments/allocate", $(%allocatePaymentRequest))



proc bwaReportApi*(httpClient: HttpClient, year: int, month: int): (Option[BWAReport], Response) =
  ## Get BWA (Betriebswirtschaftliche Auswertung) report
  var query_params_list: seq[(string, string)] = @[]
  if $year != "":
    query_params_list.add(("year", $year))
  if $month != "":
    query_params_list.add(("month", $month))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/bookkeeping/bwa" & "?" & url_encoded_query_params)
  constructResult[BWAReport](response)


proc elsterStatusApi*(httpClient: HttpClient): (Option[ElsterStatus], Response) =
  ## 

  let response = httpClient.get(basepath & "/api/v1/bookkeeping/elster/status")
  constructResult[ElsterStatus](response)


proc elsterValidateApi*(httpClient: HttpClient, zeitraum: string): Response =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("zeitraum", $zeitraum))
  let url_encoded_query_params = encodeQuery(query_params_list)
  httpClient.post(basepath & "/api/v1/bookkeeping/ustva/elster-validate" & "?" & url_encoded_query_params)



proc elsterXmlApi*(httpClient: HttpClient, zeitraum: string): Response =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("zeitraum", $zeitraum))
  let url_encoded_query_params = encodeQuery(query_params_list)
  httpClient.get(basepath & "/api/v1/bookkeeping/ustva/elster-xml" & "?" & url_encoded_query_params)



proc getCashflow*(httpClient: HttpClient, year: int, month: int): (Option[CashflowReport], Response) =
  ## GET /api/v1/bookkeeping/cashflow Returns operating, investing, and financing cashflow for the given period.
  var query_params_list: seq[(string, string)] = @[]
  if $year != "":
    query_params_list.add(("year", $year))
  if $month != "":
    query_params_list.add(("month", $month))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/bookkeeping/cashflow" & "?" & url_encoded_query_params)
  constructResult[CashflowReport](response)


proc getLiquidity*(httpClient: HttpClient): (Option[LiquidityPosition], Response) =
  ## GET /api/v1/bookkeeping/liquidity Returns current liquidity position with ratios.

  let response = httpClient.get(basepath & "/api/v1/bookkeeping/liquidity")
  constructResult[LiquidityPosition](response)


proc getOpenInvoicesApi*(httpClient: HttpClient, customerId: string): (Option[seq[Invoice]], Response) =
  ## Get open invoices for a customer

  let response = httpClient.get(basepath & fmt"/api/v1/payments/open-invoices/{customer_id}")
  constructResult[seq[Invoice]](response)


proc getVerfahrensdokumentation*(httpClient: HttpClient): (Option[Verfahrensdokumentation], Response) =
  ## GET /api/v1/bookkeeping/verfahrensdokumentation Returns the complete compliance catalog of all documented modules.

  let response = httpClient.get(basepath & "/api/v1/bookkeeping/verfahrensdokumentation")
  constructResult[Verfahrensdokumentation](response)


proc runDunningApi*(httpClient: HttpClient): (Option[DunningResult], Response) =
  ## 

  let response = httpClient.post(basepath & "/api/v1/bookkeeping/dunning")
  constructResult[DunningResult](response)

