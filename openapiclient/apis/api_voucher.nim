#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_any_type
import ../models/model_plugin_error
import ../models/model_voucher
import ../models/model_voucher_create

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc createVoucher*(httpClient: HttpClient, voucherCreate: VoucherCreate): (Option[Voucher], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/vouchers", $(%voucherCreate))
  constructResult[Voucher](response)


proc deleteVoucher*(httpClient: HttpClient, voucherId: string): Response =
  ## 
  httpClient.delete(basepath & fmt"/api/v1/vouchers/{voucher_id}")



proc getVoucher*(httpClient: HttpClient, voucherId: string): (Option[Voucher], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/vouchers/{voucher_id}")
  constructResult[Voucher](response)


proc listVouchers*(httpClient: HttpClient, page: int, pageSize: int, voucherType: string, voucherStatus: string, contactName: string, dateFrom: string, dateTo: string): (Option[seq[Voucher]], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  if $page != "":
    query_params_list.add(("page", $page))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $voucherType != "":
    query_params_list.add(("voucher_type", $voucherType))
  if $voucherStatus != "":
    query_params_list.add(("voucher_status", $voucherStatus))
  if $contactName != "":
    query_params_list.add(("contact_name", $contactName))
  if $dateFrom != "":
    query_params_list.add(("date_from", $dateFrom))
  if $dateTo != "":
    query_params_list.add(("date_to", $dateTo))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/vouchers/" & "?" & url_encoded_query_params)
  constructResult[seq[Voucher]](response)


proc updateVoucher*(httpClient: HttpClient, voucherId: string, body: JsonNode): (Option[Voucher], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.put(basepath & fmt"/api/v1/vouchers/{voucher_id}", $(%body))
  constructResult[Voucher](response)


proc voucherRestore*(httpClient: HttpClient, voucherId: string): (Option[Voucher], Response) =
  ## 

  let response = httpClient.post(basepath & fmt"/api/v1/vouchers/{voucher_id}/restore")
  constructResult[Voucher](response)

