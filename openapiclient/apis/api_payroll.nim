#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_any_type
import ../models/model_payroll_create_payload
import ../models/model_payroll_pay_payload
import ../models/model_payroll_run_api
import ../models/model_yearly_payroll_summary

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc payrollApprove*(httpClient: HttpClient, id: stringstring): (Option[PayrollRunApi], Response) =
  ## 

  let response = httpClient.post(basepath & fmt"/api/v1/payroll/{id}/approve")
  constructResult[PayrollRunApi](response)


proc payrollAutopay*(httpClient: HttpClient, id: string, body: JsonNode): (Option[JsonNode], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/api/v1/payroll/{id}/autopay", $(%body))
  constructResult[JsonNode](response)


proc payrollCalculate*(httpClient: HttpClient, id: stringstring): (Option[PayrollRunApi], Response) =
  ## 

  let response = httpClient.post(basepath & fmt"/api/v1/payroll/{id}/calculate")
  constructResult[PayrollRunApi](response)


proc payrollCreate*(httpClient: HttpClient, payrollCreatePayload: PayrollCreatePayload): (Option[PayrollRunApi], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/payroll", $(%payrollCreatePayload))
  constructResult[PayrollRunApi](response)


proc payrollDelete*(httpClient: HttpClient, id: stringstring): Response =
  ## 
  httpClient.delete(basepath & fmt"/api/v1/payroll/{id}")



proc payrollElsterExport*(httpClient: HttpClient, id: stringstring): Response =
  ## 
  httpClient.post(basepath & fmt"/api/v1/payroll/{id}/elster-export")



proc payrollEmail*(httpClient: HttpClient, id: string): (Option[JsonNode], Response) =
  ## 

  let response = httpClient.post(basepath & fmt"/api/v1/payroll/{id}/email")
  constructResult[JsonNode](response)


proc payrollEntryPdf*(httpClient: HttpClient, id: string, entryId: string): Response =
  ## 
  httpClient.get(basepath & fmt"/api/v1/payroll/{id}/entries/{entry_id}/pdf")



proc payrollGet*(httpClient: HttpClient, id: stringstring): (Option[PayrollRunApi], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/payroll/{id}")
  constructResult[PayrollRunApi](response)


proc payrollList*(httpClient: HttpClient, year: int, status: string): (Option[seq[PayrollRunApi]], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  if $year != "":
    query_params_list.add(("year", $year))
  if $status != "":
    query_params_list.add(("status", $status))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/payroll" & "?" & url_encoded_query_params)
  constructResult[seq[PayrollRunApi]](response)


proc payrollPay*(httpClient: HttpClient, id: stringstring, payrollPayPayload: PayrollPayPayload): (Option[PayrollRunApi], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/api/v1/payroll/{id}/pay", $(%payrollPayPayload))
  constructResult[PayrollRunApi](response)


proc payrollPdf*(httpClient: HttpClient, id: string): Response =
  ## 
  httpClient.get(basepath & fmt"/api/v1/payroll/{id}/pdf")



proc payrollSummary*(httpClient: HttpClient, year: int): (Option[YearlyPayrollSummary], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/payroll/summary/{year}")
  constructResult[YearlyPayrollSummary](response)


proc payrollSvMeldungen*(httpClient: HttpClient, id: stringstring): (Option[JsonNode], Response) =
  ## 

  let response = httpClient.post(basepath & fmt"/api/v1/payroll/{id}/sv-meldungen")
  constructResult[JsonNode](response)

