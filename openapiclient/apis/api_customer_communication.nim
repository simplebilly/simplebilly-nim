#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_communication_channel
import ../models/model_communication_direction
import ../models/model_contact_history_response
import ../models/model_customer_communication
import ../models/model_customer_communication_create
import ../models/model_customer_communication_update
import ../models/model_plugin_error

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc createCommunication*(httpClient: HttpClient, customerCommunicationCreate: CustomerCommunicationCreate): (Option[CustomerCommunication], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/communications", $(%customerCommunicationCreate))
  constructResult[CustomerCommunication](response)


proc customercommunicationRestore*(httpClient: HttpClient, communicationId: string): (Option[CustomerCommunication], Response) =
  ## 

  let response = httpClient.post(basepath & fmt"/api/v1/communications/{communication_id}/restore")
  constructResult[CustomerCommunication](response)


proc deleteCommunication*(httpClient: HttpClient, communicationId: string): Response =
  ## 
  httpClient.delete(basepath & fmt"/api/v1/communications/{communication_id}")



proc getCommunication*(httpClient: HttpClient, communicationId: string): (Option[CustomerCommunication], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/communications/{communication_id}")
  constructResult[CustomerCommunication](response)


proc getContactHistory*(httpClient: HttpClient, contactId: string): (Option[ContactHistoryResponse], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/contacts/{contact_id}/communications")
  constructResult[ContactHistoryResponse](response)


proc listCommunications*(httpClient: HttpClient, page: int, pageSize: int, contactId: string, channel: CommunicationChannel, direction: CommunicationDirection, `from`: string, to: string): (Option[seq[CustomerCommunication]], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  if $page != "":
    query_params_list.add(("page", $page))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $contactId != "":
    query_params_list.add(("contact_id", $contactId))
  if $channel != "":
    query_params_list.add(("channel", $channel))
  if $direction != "":
    query_params_list.add(("direction", $direction))
  if $`from` != "":
    query_params_list.add(("from", $`from`))
  if $to != "":
    query_params_list.add(("to", $to))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/communications/" & "?" & url_encoded_query_params)
  constructResult[seq[CustomerCommunication]](response)


proc updateCommunication*(httpClient: HttpClient, communicationId: string, customerCommunicationUpdate: CustomerCommunicationUpdate): (Option[CustomerCommunication], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.put(basepath & fmt"/api/v1/communications/{communication_id}", $(%customerCommunicationUpdate))
  constructResult[CustomerCommunication](response)

