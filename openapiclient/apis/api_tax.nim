#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_tax_rate_create

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc createTaxRate*(httpClient: HttpClient, taxRateCreate: TaxRateCreate): Response =
  ## Create a tax rate (`admin:settings`).
  httpClient.headers["Content-Type"] = "application/json"
  httpClient.post(basepath & "/api/v1/tax-rates", $(%taxRateCreate))



proc deleteTaxRate*(httpClient: HttpClient, id: stringstring): Response =
  ## Delete a tax rate by id (`admin:settings`).
  httpClient.delete(basepath & fmt"/api/v1/tax-rates/{id}")



proc listTaxRates*(httpClient: HttpClient): Response =
  ## List the calling tenant's tax rates.
  httpClient.get(basepath & "/api/v1/tax-rates")



proc updateTaxRate*(httpClient: HttpClient, id: stringstring, taxRateCreate: TaxRateCreate): Response =
  ## Update a tax rate by id (`admin:settings`). Replaces all body fields.
  httpClient.headers["Content-Type"] = "application/json"
  httpClient.put(basepath & fmt"/api/v1/tax-rates/{id}", $(%taxRateCreate))


