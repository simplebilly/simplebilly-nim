#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_sepa_direct_debit_response

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc createSepaDirectDebitApi*(httpClient: HttpClient, creditorName: string, creditorIban: string, creditorId: string, mandateId: string, mandateDate: string, debtorName: string, debtorIban: string, amount: string, collectionDate: string, creditorBic: string, debtorBic: string, description: string): (Option[SepaDirectDebitResponse], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("creditor_name", $creditorName))
  query_params_list.add(("creditor_iban", $creditorIban))
  if $creditorBic != "":
    query_params_list.add(("creditor_bic", $creditorBic))
  query_params_list.add(("creditor_id", $creditorId))
  query_params_list.add(("mandate_id", $mandateId))
  query_params_list.add(("mandate_date", $mandateDate))
  query_params_list.add(("debtor_name", $debtorName))
  query_params_list.add(("debtor_iban", $debtorIban))
  if $debtorBic != "":
    query_params_list.add(("debtor_bic", $debtorBic))
  query_params_list.add(("amount", $amount))
  query_params_list.add(("collection_date", $collectionDate))
  if $description != "":
    query_params_list.add(("description", $description))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.post(basepath & "/api/v1/bookkeeping/sepa-direct-debit" & "?" & url_encoded_query_params)
  constructResult[SepaDirectDebitResponse](response)

