#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_any_type
import ../models/model_email_template
import ../models/model_email_template_create
import ../models/model_email_template_update
import ../models/model_plugin_error

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc createEmailTemplate*(httpClient: HttpClient, emailTemplateCreate: EmailTemplateCreate): (Option[EmailTemplate], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/email-templates", $(%emailTemplateCreate))
  constructResult[EmailTemplate](response)


proc deleteEmailTemplate*(httpClient: HttpClient, emailTemplateId: string): Response =
  ## 
  httpClient.delete(basepath & fmt"/api/v1/email-templates/{email_template_id}")



proc getEmailTemplate*(httpClient: HttpClient, emailTemplateId: string): (Option[EmailTemplate], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/email-templates/{email_template_id}")
  constructResult[EmailTemplate](response)


proc listEmailTemplates*(httpClient: HttpClient, page: int, pageSize: int, status: string, search: string): (Option[seq[EmailTemplate]], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  if $page != "":
    query_params_list.add(("page", $page))
  if $pageSize != "":
    query_params_list.add(("pageSize", $pageSize))
  if $status != "":
    query_params_list.add(("status", $status))
  if $search != "":
    query_params_list.add(("search", $search))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/email-templates/" & "?" & url_encoded_query_params)
  constructResult[seq[EmailTemplate]](response)


proc renderEmailTemplate*(httpClient: HttpClient, emailTemplateId: string, body: JsonNode): (Option[JsonNode], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & fmt"/api/v1/email-templates/{email_template_id}/render", $(%body))
  constructResult[JsonNode](response)


proc updateEmailTemplate*(httpClient: HttpClient, emailTemplateId: string, emailTemplateUpdate: EmailTemplateUpdate): (Option[EmailTemplate], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.put(basepath & fmt"/api/v1/email-templates/{email_template_id}", $(%emailTemplateUpdate))
  constructResult[EmailTemplate](response)

