#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_deliverable_response
import ../models/model_plugin_error
import ../models/model_shipping_threshold
import ../models/model_shipping_threshold_create
import ../models/model_shipping_threshold_update

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc createShippingThreshold*(httpClient: HttpClient, shippingThresholdCreate: ShippingThresholdCreate): (Option[ShippingThreshold], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/shipping-thresholds", $(%shippingThresholdCreate))
  constructResult[ShippingThreshold](response)


proc deleteShippingThreshold*(httpClient: HttpClient, thresholdId: string): Response =
  ## 
  httpClient.delete(basepath & fmt"/api/v1/shipping-thresholds/{threshold_id}")



proc getDeliverable*(httpClient: HttpClient, productId: stringstring, warehouseId: string): (Option[DeliverableResponse], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  query_params_list.add(("productId", $productId))
  if $warehouseId != "":
    query_params_list.add(("warehouseId", $warehouseId))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/shipping-thresholds/deliverable" & "?" & url_encoded_query_params)
  constructResult[DeliverableResponse](response)


proc getShippingThreshold*(httpClient: HttpClient, thresholdId: string): (Option[ShippingThreshold], Response) =
  ## 

  let response = httpClient.get(basepath & fmt"/api/v1/shipping-thresholds/{threshold_id}")
  constructResult[ShippingThreshold](response)


proc listShippingThresholds*(httpClient: HttpClient, page: int, pageSize: int, productId: stringstring, warehouseId: string, isActive: bool): (Option[seq[ShippingThreshold]], Response) =
  ## 
  var query_params_list: seq[(string, string)] = @[]
  if $page != "":
    query_params_list.add(("page", $page))
  if $pageSize != "":
    query_params_list.add(("page_size", $pageSize))
  if $productId != "":
    query_params_list.add(("product_id", $productId))
  if $warehouseId != "":
    query_params_list.add(("warehouse_id", $warehouseId))
  if $isActive != "":
    query_params_list.add(("is_active", $isActive))
  let url_encoded_query_params = encodeQuery(query_params_list)

  let response = httpClient.get(basepath & "/api/v1/shipping-thresholds/" & "?" & url_encoded_query_params)
  constructResult[seq[ShippingThreshold]](response)


proc updateShippingThreshold*(httpClient: HttpClient, thresholdId: string, shippingThresholdUpdate: ShippingThresholdUpdate): (Option[ShippingThreshold], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.put(basepath & fmt"/api/v1/shipping-thresholds/{threshold_id}", $(%shippingThresholdUpdate))
  constructResult[ShippingThreshold](response)

