#
# SimpleBilly API
# 
# Simplebilly API - Bookkeeping, CRM, ERP. Multi-tenant API: a tenant is isolated and routed by subdomain (or a configured custom domain) under the base domain.  ## Rate limiting All endpoints are rate-limited per client IP: **100 requests per minute** on API routes and **5 requests per minute** on authentication routes. Exceeding a limit returns `429 Too Many Requests`; the window resets after 60 seconds.
# The version of the OpenAPI document: 0.1.0
# Contact: support@simplebilly.com
# Generated by: https://openapi-generator.tech
#

import httpclient
import json
import logging
import marshal
import options
import strformat
import strutils
import tables
import typetraits
import uri

import ../models/model_any_type
import ../models/model_gateway_o_auth_authorize_request
import ../models/model_gateway_o_auth_authorize_response
import ../models/model_gateway_o_auth_callback_request
import ../models/model_payment_gateway
import ../models/model_plugin_error

const basepath = "https://demo.simplebilly.com"

template constructResult[T](response: Response): untyped =
  if response.code in {Http200, Http201, Http202, Http204, Http206}:
    try:
      (some(to(parseJson(response.body), T)), response)
    except JsonParsingError:
      # The server returned a malformed response though the response code is 2XX
      # TODO: need better error handling
      error("JsonParsingError")
      (none(T.typedesc), response)
  else:
    (none(T.typedesc), response)


proc createPaymentGatewayApi*(httpClient: HttpClient, body: JsonNode): (Option[PaymentGateway], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/payment-gateways", $(%body))
  constructResult[PaymentGateway](response)


proc deletePaymentGatewayApi*(httpClient: HttpClient, gatewayId: string): Response =
  ## 
  httpClient.delete(basepath & fmt"/api/v1/payment-gateways/{gateway_id}")



proc listPaymentGatewaysApi*(httpClient: HttpClient): (Option[seq[PaymentGateway]], Response) =
  ## 

  let response = httpClient.get(basepath & "/api/v1/payment-gateways/")
  constructResult[seq[PaymentGateway]](response)


proc oauthAuthorizeApi*(httpClient: HttpClient, gatewayOAuthAuthorizeRequest: GatewayOAuthAuthorizeRequest): (Option[GatewayOAuthAuthorizeResponse], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/payment-gateways/oauth/authorize", $(%gatewayOAuthAuthorizeRequest))
  constructResult[GatewayOAuthAuthorizeResponse](response)


proc oauthCallbackApi*(httpClient: HttpClient, gatewayOAuthCallbackRequest: GatewayOAuthCallbackRequest): (Option[PaymentGateway], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.post(basepath & "/api/v1/payment-gateways/oauth/callback", $(%gatewayOAuthCallbackRequest))
  constructResult[PaymentGateway](response)


proc updatePaymentGatewayApi*(httpClient: HttpClient, gatewayId: string, body: JsonNode): (Option[PaymentGateway], Response) =
  ## 
  httpClient.headers["Content-Type"] = "application/json"

  let response = httpClient.put(basepath & fmt"/api/v1/payment-gateways/{gateway_id}", $(%body))
  constructResult[PaymentGateway](response)

